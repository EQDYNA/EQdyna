# Pathway Forward

Record of tasks done and open issues for this repo. See
`PROJECT_RULES.md` for the rules each item enforces. History (what shipped,
when) lives in `pastReleaseNotes.md` and `README.md` — this file does not
duplicate it and is not append-only; items are updated or closed in place.

A blank "Last checked" means never audited and stays blank until someone
actually runs the command.

**Struck-through (resolved) rows record the state AT THE TIME they were
closed**, so they name files that have since been deleted or moved -- notably
`check.test.py`, `testAll.py`, `src/pydump.f90`, the `accept` and `parity`
tiers, and the pre-restructure Python module names (`port_rsf.py`,
`kernels_numpy.py`, `standalone/main.py`). That is history, not drift, and it
is not rewritten. The `Command` column of an OPEN row, by contrast, must run
today -- if one does not, that is a bug in this file.

Current layout, for reading the older rows: Fortran is `src/fortran/`, the
Python solver is `src/python/eqdyna/` (14 modules, one per Fortran file), and
there is ONE test -- `testsys/run.py e2e`, 8 cases x 3 backends = 24 cells.

## Priority order (set 2026-09-16)

Ranked by value per unit of risk, not by item number. P1 first; do not start a
P3 whose prerequisite is unmet. **BLOCKED-ON-OWNER items need a decision, not
effort** -- doing them anyway means guessing at something that is not mine to
guess.

| rank | item | what | why this rank | blocked on |
|---|---|---|---|---|
| **P1** | 32 | MEASURED 2026-09-16, and the additive model is REFUTED. drv.a6's rupture-arrival flips are a FIXED MARGINAL POPULATION, not a sum of error sources. The experiment: compare serial python against a SERIAL FORTRAN reference, removing the decomposition term by construction. Prediction was ~90 flips (port alone) after subtracting a ~330 decomposition floor. **Removing decomposition made it WORSE.** fortran-serial vs 4-rank **372**; python-jax vs 4-rank **415**; python-numpy vs 4-rank **423**; python-jax vs fortran-serial **442**; python-numpy vs fortran-serial **450** (exactly AT the budget). Every pair lands in 372-450 of 5151 nodes (7-9%), and every pair has median |dfnft| = **0.0417 s = exactly one time step**. That is the signature of ~400 nodes whose arrest is decided at roundoff: ANY two runs differing at all -- rank count, language, summation order -- disagree on most of them, and the count measures the POPULATION SIZE, not the perturbation magnitude. So '329 decomposition + 90 port' was a model the data does not support, and neither does 'the port is responsible for 415'. TWO CORRECTIONS TO THE RECORD: (i) this session earlier reported **329** for fortran-serial vs 4-rank; it reproduces at **372**, matching item 18/21's recorded A=372. The 329 has no recoverable provenance and is DISCARDED, not averaged. (ii) python-numpy vs a serial reference is exactly 450/450, so if drv.a6's reference were ever re-frozen from a serial run the gate would sit at its limit with zero headroom -- the 450 budget is specific to the 4-rank reference it was calibrated against. WHAT WOULD ACTUALLY SETTLE THE PHYSICS: refine the mesh. If the marginal population is a discretisation artifact its FRACTION shrinks at dx=250; if the fault is genuinely bistable at this roughness and friction, the fraction persists. That is now the open question, and it is a different experiment from the one this item used to propose. Reproduce with `testsys/parity/evidence_drv_a6_marginal_population.py`. | 2, 4, 5 | `testsys/matrix.py` DRV_A6, `testsys/parity/evidence_drv_a6_marginal_population.py` | until refined | 2026-09-16 | `python3 testsys/parity/evidence_drv_a6_marginal_population.py` |
| **P1** | 19(a) | gate TPV36 + TPV37 | compsets and code branches already exist; **no gated case exercises `C_degen`** (verified across all 8), and both set `C_degen = par.dip`, so this closes a path that ships untested | BLOCKED, and the scope call has changed twice in one day. `73c6961` (on master) ported the wedge MESH only and REFUSES the dynamics -- correct, and still the shipped state. **FOUND 2026-09-16 (this session): an UNCOMMITTED dynamics port already exists**, sitting in the stale worktree `.claude/worktrees/agent-a4a8ccb6ca1ec356e` (HEAD `6d1c98b`, not stale vs the 3 files it touches -- checked). It is a genuinely careful draft: `compute_element_shape` gets the `calcGlobalShapeFunc.f90:22-28` row merge, a NEW `_contm_wedge_node_mass` replaces `contm`'s closed form for wedge nodes (correctly identifying that the closed form's `localShapeFunc(4,j)==1/8`-uniform precondition breaks under the merge -- this session's own scoping guess, confirmed right), and it independently found and fixed a SECOND gap the original refusal never named: `assembleGlobalKU.py`'s `E_int` dispatch excluded elemType>10, so wedge elements would have contributed zero interior force while still contributing mass. **DO NOT LAND AS-IS.** Its own code comments claim it was "verified with a fresh few-step Fortran-vs-Python diff" -- there is no such diff. I ran its own `testsys/parity/evidence_c_degen_port.py` myself, in that worktree: item (a) mesh parity still EXACT MATCH; item (b) now runs 5 numpy dynamic steps without raising, BUT `attempt_dynamic()` only checks whether the call raises -- **zero comparison against any Fortran number, at any step, for any wedge quantity.** "Doesn't crash" is not evidence for a kernel whose failure mode is a silent wrong mass split. (Also checked: a `RuntimeWarning: divide by zero` this run prints is pre-existing/documented/intentional -- `faulting.f90`'s r==nucR taper edge case, unrelated to wedge dynamics, confirmed by reading `faulting.py:100-106`'s own docstring -- not a new defect.) The worktree is preserved on disk specifically because of this; do not reap it. Next step for whoever picks this up: extend the item (a)-style Fortran probe to dump a wedge element's mass/force at step 1 and diff it, THEN decide. **DECIDED 2026-09-16 (relayed via coordinator, corroborated independently: a live, locked worktree `agent-ac2df7291e93fd4ba` based on the current release commit `afc842b` exists on disk, matching the claimed dispatch):** the dynamics port proceeds -- routed to mira-volkov in her own worktree, scope = build the missing wedge mass/force probe against Fortran (the real evidence the abandoned draft never had) then tpv36 parity on all three backends against its committed reference. She is under orders not to register tpv36/tpv37 in `testNameList.py`/`matrix.py` herself -- gating is still a separate decision after her parity work lands, not automatic. Not independently confirmed by a direct owner statement (I have no channel to verify that beyond the coordinator's relay and the worktree's existence); recorded as the operating decision going forward, not re-litigated. **UPDATE 2026-09-16: mira-volkov's port landed (commit `9ea6f17`), then REVERTED (`75a993e`) minutes later.** Her kernel-probe verification (Fortran-vs-Python on synthetic elements, rtol=1e-10) was real and passed, and tpv36 gated bit-exact on fortran plus roundoff-level diffs on numpy/jax against its own frozen reference -- but the merged tree's FULL fresh e2e sweep (9 cases/27 cells, run after both this and haruto's CI-matrix merge) found `test.tpv8 x python-jax` newly failing its bound by 10x (1.0e-07 vs 1.0e-08, row 940 col 13) -- a case with ZERO wedge elements, whose bound had held at ~4e-10 all session. Isolated by direct experiment, not inferred: reverting ONLY `assembleGlobalKU.py`/`assembleGlobalMass.py`/`eqdyna3d.py` to their pre-merge (`afc842b`) content and rerunning `test.tpv8 x python-jax` reproduces the old clean 4.119873e-10; restoring the merged files reproduces the 1.0e-07 failure -- confirmed both directions. Root cause NOT fully pinned: a synthetic microbenchmark of the einsum-signature change in `compute_element_shape` (`'ik,ekj->eji'` -> `'eik,ekj->eji'`, needed for the per-element wedge/non-wedge derivative select) came back bit-identical for non-wedge elements in isolation, so the actual divergence is somewhere else in the same 3 files -- plausibly a JAX-specific effect (XLA may fuse/schedule the whole traced graph differently once elemType-conditional branches exist in it, even on a mesh where every branch evaluates false, unlike NumPy's eager per-op execution -- consistent with python-numpy NOT failing while python-jax did). Sent back to mira-volkov with this exact isolation recipe rather than the port itself, since finding the precise mechanism is a real debugging task, not a re-review. Do not re-attempt the merge without a fresh full 9-case/27-cell sweep confirming tpv8 x jax (and every other non-wedge cell) unchanged. Also blocked on reference-freeze-creates-new-ground-truth (rule 7). **RESOLVED 2026-09-16 (fresh worktree, re-derived from master `ad48856`, since the debugging worktree above was already reaped): root cause PINNED, not the jax-scheduling hypothesis.** `compute_element_shape`'s `eleshp` contraction used ONE per-element einsum (`'erc,eck->erk'`) for every element regardless of wedge/non-wedge, replacing the pre-wedge-port invariant-operand einsum (`'erc,ck->erk'`). On test.tpv8's real mesh (235,008 elements, zero wedge) this is a genuine NumPy bug, not a jax artifact: measured directly by loading the OLD and NEW `compute_element_shape` as standalone modules against the SAME real tpv8 `xl`/`elem_type` arrays, `eleshp` differs (max|diff|=2.168e-19, 18,462/235,008 elements) purely from NumPy's einsum optimizer dispatching the invariant-operand form to a BLAS batched matmul (FMA) and the per-element form to a generic no-FMA loop. `compute_element_shape` runs ONCE, host-side, in plain NumPy for EITHER backend (`eqdyna3d.py`'s `build_solver_state`, never under `jax.jit`) -- confirmed by reading the call site, not assumed -- so the divergence and its amplification-difference are backend-agnostic; only the 114-step nonlinear rupture dynamics happened to amplify the same tiny perturbation past jax's bound (1e-8) while numpy stayed under by chance. The earlier "XLA schedules a per-element-conditional graph differently" hypothesis does NOT hold and is retracted. FIX: `compute_element_shape` now dispatches the `eleshp` contraction BY MASK -- non-wedge elements get the exact pre-wedge-port einsum back (confirmed bit-identical to the old code on tpv8's real mesh), wedge elements keep the per-element form. VERIFIED: `test.tpv8 x python-jax` back to `4.119873e-10` (was `1.0e-07`), a fresh full `python3 testsys/run.py all` (unit 236, regression, e2e 24/24 cells on the 8-case table) all green, and `testsys/parity/evidence_wedge_kernel.py` (numpy AND a NEW jax-cpu/jax.jit column, closing the "probe never exercised jax" gap named when this item was reopened) passes fortran-vs-numpy-vs-jax on hex/wedge_degenerate/wedge_shallow_dip15. tpv36 re-registered (`testNameList.py`, `testsys/matrix.py`, bound `1e-6`) against a FRESH reference (frozen from a clean `create.newcase`+`case.setup`+4-rank `mpirun`+`plotRuptureDynamics` run on this commit's Fortran and code, not reused from `9ea6f17`'s committed artifact -- `fault.dyna.r.nc` came out byte-identical at 349723 bytes, `frt.canonical.txt` at 3477 rows, consistent since the Fortran side never changed). Fresh 9-case/27-cell full sweep: 27/27 SUCCESS, `test.tpv36 x python-jax` `5.867293e-09`, `test.tpv36 x python-numpy` `7.894064e-09` (both well inside the new bound). One side effect the fresh sweep caught that the earlier merge's did NOT (a genuinely new regression guard, not a repeat): `matrix.CI_CELLS` includes every case's fortran cell unconditionally, so gating tpv36 silently widened CI_CELLS by one cell (`test.tpv36 x fortran`) with no workflow job selecting it -- `test_ci_workflow_coverage.py` caught this immediately (a real regression test doing its job, not a false alarm); fixed by adding `test.tpv36` to CI's fortran "group B" job and updating the affected cell-count comments (`.github/workflows/test.yml`, `CLAUDE.md`, `matrix.py`, now 11 of 27). Branch `fix/tpv8-jax-shape-regression` off `ad48856`, not pushed -- awaiting the orchestrating session's gate-axis-3/4 review before merge. **MERGED 2026-09-16 (v5.9.0):** reviewed against current HEAD (clean, no staleness -- master had not touched any file this branch touches), her fix independently re-verified (fresh `test.tpv8 x python-jax` in her worktree, `4.119873e-10`), then merged onto master alongside haruto-nakamura's independent CI_CELLS widening (10->19 of 24) -- both branches had diverged from each other, reconciled by hand (`testsys/matrix.py`'s CI_CELLS comment, `CLAUDE.md`'s cell count) to the true combined state of 20 of 27, `.github/workflows/test.yml` and `pathway_forward.md` auto-merged cleanly. Fresh full gate on the fully-merged tree: 27/27 SUCCESS. Item 19(a) is now CLOSED for TPV36; TPV37 (no compset exists yet) remains open, see item 19's own row. **UPDATE 2026-09-17 (v5.10.0): TPV37 closed too.** Registration only, no port work needed -- both `src/fortran/faulting.f90:405` and `src/python/eqdyna/faulting.py:109` already carried the `TPV==37` branch from TPV36's own landing. Gated via `git cherry-pick --no-commit` from mira-volkov's worktree (`agent-a19d02377f7a99d91`), re-verified fresh before landing: `python3 testsys/e2e/run_e2e.py --cases test.tpv37,test.tpv36 --backends fortran,python-numpy,python-jax` 6/6 SUCCESS, fortran bit-exact both cases, tpv37 numpy/jax 6.929140e-09/5.256410e-09 against bound 1e-6, tpv36 unmoved at 7.894064e-09/5.867293e-09. Item 19(a) is now CLOSED for both TPV36 and TPV37; see item 19's own row and this release's Tasks-done entry. |
| **P2** | 23 | isolated Drucker-Prager kernel test | ~~friclaw 4 is covered end-to-end (drv.a6, tpv104) but never in isolation~~ RESOLVED (`d5da43e`, verified fresh 2026-09-16 by this session, second independent run since landing): `testsys/regression/test_drucker_prager_kernel.py` compiles a driver against the real unmodified `calcElemKU.f90:127-161` (gfortran, no MPI/case/mesh) and diffs 4 synthetic stress states against the Python port to rtol=1e-12/atol=1e-9. `SUCCESS` both times. Picked up by `testsys/run.py regression`'s existing glob (confirmed: fresh `unit regression` tier run this session, exit 0, both tiers SUCCESS) | nothing |
| **P2** | 28 | ~~TPV29's cross-code validation numbers are prose-only~~ RESOLVED (2026-09-16, `5d1a428` + `d5da43e`) by tracking the TOOLING (`scripts/scec/`, 116 KB, regenerates the 484 MB gitignored baseline) AND landing the comparison script itself, `testsys/parity/evidence_tpv29_scec_comparison.py` (report-only, never a gate). Verified fresh 2026-09-16 by this session (second independent run since landing, `evidence_output/evidence_tpv29_20260916T191603Z.json` then re-run after the correction below): of the four recorded prose numbers, the script **falsified one on its first run, not reproduced it** -- read that as item 28 doing its job, not as item 28 failing. Median rupture-time diff 0.0080s (recorded 0.006, drifted by one output-cadence step); final-slip 0.2247% (recorded 0.22%, reproduces); ruptured area 743.5 km² by the script's primary corner-rule metric vs the recorded 749-756 km² range (drifted) but 749.5 km² by the naive node-count rule (reproduces almost exactly) -- **not a physics discrepancy, a metric-definition mismatch**: the recorded figure was produced by node-counting, not the corner rule, now labelled in the script rather than "fixed". **Mw was a real error, corrected**: recorded 7.45 has no reproducible provenance and is physically impossible for this run -- Kanamori Mw=(log10(M0)-9.1)/1.5 puts 7.45 at M0=1.884e20 N·m, 4.7x this run's actual M0=3.990e19 N·m over 80601 deduped fault nodes, requiring mean slip ~6.5 m over every node against this run's own max of 2.903 m. `RECORDED['mw']` corrected to 7.034 (this run's own measured value, now the baseline itself, not a second inherited claim) in the script with the derivation in-line; `pathway_forward.md` item 20 below corrected to match. Not chased further, same call as item 32's unrecoverable 329-flip figure: wrong numbers get corrected and the session moves on, not archaeology. | 2, 4 | `scripts/scec/`, `testsys/parity/evidence_tpv29_scec_comparison.py`, `case_input/test.tpv29/README.md` | resolved 2026-09-16 | `python3 testsys/parity/evidence_tpv29_scec_comparison.py` |
| **P2** | 33 | re-measure JAX-CPU core scaling on an idle box | every scaling number quoted today is stale or load-contaminated; the inherited knee at 8 cores coincides exactly with the NUMA node size, so it may be measuring locality not the solver. **UPDATE 2026-09-17: two tooling defects found+fixed in `testsys/perf/run_numa_scaling.py` this session (commit `03ba055`)** -- an all-64-core config contradicting item 33's own "at most 32" constraint (removed), and a 4x gap between spread-8 and socket-32 with no 16-core point in exactly the region this item asks about (added a generic two-NUMA-node point). Verified by `ast.parse` + `--help` only, no idle box available to run the measurement itself; load-ceiling/override logic untouched. The tool was wrong before today and would have misled even on an idle box -- now fixed, measurement itself still outstanding. **UPDATE 2026-09-17 (commit `d70811d`): MEASURED, ROOT-CAUSED, PARTIALLY FIXED -- real progress, not another tooling pass.** Goal reframed by the owner mid-session to "make jax and numpy scale well" (measurement was always instrumental to that, not the deliverable); landed via mira-volkov, verified by wei-lin before landing. `run_scaling.py` rewritten: `PY_THREADS` extended to match `FORTRAN_RANKS` (`[1,2,4,8]` -> `[1,2,4,8,16,32]`, so the tool can finally say whether either python backend reaches 32 cores); bare `taskset` replaced by `numactl --cpunodebind/--membind` on both the python and Fortran/MPI paths (a per-rank numactl-rankfile mechanism for MPI, since a bare `mpirun --bind-to` can be reissued past an outer numactl restriction by OpenMPI's own hwloc binder); compact vs spread placement added to attribute a knee to locality vs work exhaustion; reuses (not duplicates) `run_numa_scaling.py`'s busy-check. Measured (caveat: `--busy-ceiling 0.9` override, not the tool's strict 0.2 default, box held ~9 other tenants all session; cross-validated two independent ways, exact ms/step to be retaken on an idle box): **jax gains a real 2.35x at 32 cores** (cross-checked against this session's clean 1-4-core numbers); **numpy anti-scales, never faster than 1 core**, mechanism verified by direct microbenchmark -- OpenBLAS reads its thread count at `import numpy` time only (env-var-after-import vs numactl-pinned-before-start, tested directly, not asserted). JAX's HLO graph dumped and read directly (one `fori_loop`, 1717 fusion + 556 scatter ops on arrays up to `f64[3818584]`/step): real, large-scale structural parallelism exists, ruling out "nothing to parallelize" as the plateau's cause past 8 cores; memory bandwidth and/or the ~556 per-step scatter-accumulations are the more likely binding constraint, not chased further. Both known project failure modes (HLO-literal-cost from a jitted closure; `jax.jit` rebuilt inside a run function) were checked directly against `backend.py`/`eqdyna3d.py` and ruled out, not assumed clean. **Fix landed:** `_narrow_numpy_affinity()` in `src/python/eqdyna/eqdyna3d.py` (numpy backend only) pins the process to a single cpu, bounding the NUMA-migration worst case (a wide-affinity single-threaded process can be OS-migrated between nodes mid-run, stranding its memory -- measured ~2x regression from this alone). This does NOT make numpy scale -- its hot kernels (`calcHourglassResist`/`assembleGlobalKU` elementwise ops, `np.add.at` scatter-adds, 85%+ of a step per item 40's profile) stay single-threaded regardless of core count; a real fix needs a kernel rewrite, explicitly out of scope here. **A real regression caught by wei-lin's review, not the subagent's own testing:** the first affinity fix picked `min(current)` -- the same lowest cpu for every process, verified directly (`/proc/<pid>/status` on two concurrent processes both showed `Cpus_allowed_list: 0`) to collide every concurrent numpy cell in `testsys/e2e/run_e2e.py --jobs N>1` (this project's `run.py all`) onto one physical core while dozens sit idle, exactly explaining a ~10%-cpu-each pileup seen earlier in the session. Fixed to `ordered[os.getpid() % len(ordered)]` (still narrows to one cpu, now spreads across processes -- re-verified: two concurrent processes on cpus 25/27, not both 0). **Verification independent of the subagent's report:** her own mission disclosed only 1 of 10 numpy cells (tpv8) re-checked before she had to kill her `run.py all` under this session's box contention; wei-lin ran the remaining 9 fresh on the corrected code (`drv.a6, tpv10, tpv104, tpv1053d, meng2023a, meng2023cb, tpv29, tpv36, tpv37`, python-numpy): 9/9 SUCCESS against their existing bounds, unmoved. jax/fortran cells are provably unreachable by this change (gated on `backend=='numpy'` in the diff itself), correctly not re-run. Diffed every changed file against current master before landing (zero staleness); fresh `python3 testsys/run.py unit regression`: SUCCESS both tiers. **Still open, not urgent:** re-measure the scaling curve on a genuinely idle box for trustworthy ms/step numbers (this session never had one); jax's plateau past 8 cores is diagnosed as structural but not further isolated to memory-bandwidth vs scatter-accumulation specifically -- a controlled follow-up, not queued. | needs an IDLE box for final ms/step numbers; kernel-level numpy scaling is a separate, unscoped rewrite |
| **P2** | 29 | two changes stashed in the v5.5.0 audit | RESOLVED for all 4 stashes as of 2026-09-16 (read-only inspection, nothing popped/applied/dropped). Verified myself, matching every number: `git stash show --stat` on all four, plus `git cat-file -p stash@{N} \| grep -c '^parent '` = 2 for all four (no `-u`, no hidden untracked payload). **All four are unapplicable by construction** -- the only files carrying real content across all four (`src/pydump.f90`, `testsys/perf/run_elem_scaling.py`, `src/makefile`, `testAll.py`) are all GONE from the current tree (confirmed: none exist). `stash@{1}`/`{2}`/`{3}` are ~30-file mode-only churn (100644->100755 exec bit on `scripts/`, `install-eqdyna.sh`, `PurpleColormaps.mat` -- a checkout artifact, not authored work; `c4b13e5` shows this class of churn has bitten the repo before) plus 1-4 real content lines each in the two now-deleted files, EXCEPT `stash@{3}` also deletes `case_input/test.drv.a6/user_defined_params.py` (173 lines) -- drv.a6 is now a GATED case with a live params file and flip-budget bound, so applying that stash would delete a live case configuration. `stash@{0}` (2026-09-14) was already known-broken WIP (its own message: "CORE_BASE dead + false pinning rationale"), both its target files also now gone. **CLOSED 2026-09-17: owner approved the drop and it has been executed.** Verified fresh this session: `git stash list` on current HEAD returns EMPTY -- all four stashes gone. | nothing -- closed |
| **P3** | ~~24(b)-(f)~~ | RESOLVED 2026-09-17 (b)(c)(e)(f); (d) closed by owner decision 2026-09-16. Tv, the deviatoric depth taper and the plastic-strain output window are real case inputs now, each defaulting to the constant the solver used to compile in, and `scripts/lib.py` reads the case's own shear modulus. See item 24's row for the measurements | prerequisite for TPV30, now met | nothing |
| **P3** | 19(b) | promote TPV30 from `scratch/tpv30/` | it is the rough VISCOPLASTIC benchmark; the loading gaps it would have pinned (item 24(b)(c)) are closed as of 2026-09-17, so it is now unblocked -- the case still needs its compset, ONE frozen reference, and all three backends green (rule 17) | **CLOSED AS OPEN, 2026-09-17 (sixth and final pass today; this block replaces five same-day correction paragraphs with one consolidated summary -- do not read it as a sixth correction on top of them). OWNER DECISION, final for now: leave this investigation OPEN. Neither proposed fix lands. `test.tpv30` stays UNREGISTERED.**

Rule 17 steps 1-3, 5-6 are done (compset, `swtwNucleation` branch, spec-tier recording, and the SCEC-comparison script all landed and are unaffected by this closure); steps 4 and 7 (freeze, all-three-backends-pass) remain unmet and are what stay blocked.

**Established by measurement -- nobody re-chases these:** initial stress tensor -- `case.setup`'s B11/B33/B13 form and `setPlasticStress`'s R/theta form agree to 1e-6 relative, component-wise. Spec conformance -- the spec's 93%-of-yield-at-hypocenter-depth check reproduces exactly, both code paths. Plasticity params -- cohesion, bulk friction, Tv, depth taper, deviatoric ratio all verified against the spec. `str1ToFaultAngle=40.375111` -- correct per the B-coefficient derivation. Rough geometry file -- per-node gradients bit-identical from source file to solver (copied, not recomputed). Fault morph -- max |y_mesh - y_file| = 5.0e-04 m over all 3321 fault nodes, against +/-1.7km of relief. `arn`/`massSlave`/`massMaster` -- 0.000e+00 relative difference across all 1921 common fault nodes vs. tpv29. `un`/`us`/`ud` -- bit-identical between tpv29 and tpv30. FE force assembly -- CORRECT, independently rebuilt from the morphed surface's own facet geometry (4 adjacent quads, each facet's own normal and area, `S.n_f*A_f` with 1/4-per-quad weighting) and matched the code's output to <0.2% per force component, <0.03% on area. Gravity body force -- cancels out of the fault traction exactly (the split-node pair's contributions are equal and opposite). Interior equilibrium -- tpv30's interior-element force residual has mean 3e-04 m/s2, the bulk field is fine. Boundary tractions -- missing (Method 2's own spec requirement, never implemented) but MINOR in magnitude, mean residual only 0.09 m/s2, not the dominant effect. Strike-vector formula -- independently disproven as a hypothesis: `us=[1,pfx,0]/sqrt(1+pfx^2)` is used correctly by the analytic path too (produces tpv29's exact correct value), not a bug. `replaceSlaveWithMasterNode` -- independently disproven as a hypothesis: its dy-based check is geometrically correct for a fault at nominal y=0, the routine has no `C_elastic` dependency so it cannot explain a tpv29-vs-tpv30 difference, and this is consistent with the bit-identical mass measurement above. dx=200 geometry validation -- refused for BOTH tpv29 and tpv30 identically (2 of 202 boundary nodes over a one-sided truncation-error bound in `validateFaultRoughGeometry`), not tpv30-specific, a separate, minor, unexamined issue.

**The open defect, stated plainly as UNRESOLVED: a fault-local equilibrium failure in the `C_elastic=0` path, present in BOTH plastic cases (`test.tpv30` AND `test.drv.a6`).** Class 2 (near-fault elements): tpv30 max residual 594 m/s2 / mean 5-13.5; drv.a6 max residual 395 m/s2 / mean 9.8. Class 1 (interior elements): tpv30 mean 3e-04 m/s2; drv.a6 mean 0.044 m/s2 -- 150x worse than tpv30's interior, ALSO unexplained. Symptom at `faultst000dp120`, dx=500: t=0 shear reads 33.051 MPa and relaxes -4.30 MPa over ~2s onto ~28.75 MPa. Target is 27.791 MPa (the prescribed `S.n`, which `test.tpv29` produces exactly and which the owner's own published cross-code data confirms is correct). The decisive evidence this is a real defect, not a discretization property: the owner's three published TPV30 submissions (100m/50m/25m) are RESOLUTION-INDEPENDENT (t=0 shear 28.176/27.888/27.815 MPa, only 1.3% spread over 4x mesh refinement) and show NO relaxation at ANY resolution (pre-arrival change +0.636/+0.640/+0.636 MPa, essentially identical) -- all converging toward 27.791. A discretization artifact would be a static offset that grows with element size; this is neither static (it relaxes) nor growing with coarseness (it's resolution-independent), so Method 2 (which `test.tpv30` correctly runs per spec) is proven to work correctly at every published resolution -- the defect is something else, specific to this build, not yet located despite seven measured-and-rejected hypotheses (see the exonerated list above).

**Two fix attempts were built, verified, and REJECTED -- do not resurrect either without new evidence:** (1) a discrete-equilibrium correction-offset (`T_init = analytic - FE reconstruction`, computed once at step 1, held constant) -- numerically verified to hit the target (27.7906-27.7915 MPa stable across the full pre-arrival window) but rejected: it cancels a symptom without finding its cause, and the resolution-independence evidence above means there IS a real cause to find. (2) A literal Method-1 architectural switch (drop gravity, use the element field for yield only, apply `T_init` directly) -- also rejected once the resolution-independence evidence came in, since it would abandon a method (Method 2) now proven correct, to dodge a defect that hasn't been located. Both implementation worktrees exist but neither lands; both approaches were tried and rejected, no further identification needed.

**The stakes -- `test.drv.a6` is GATED and running in CI today with a reference that encodes this exact unresolved behavior.** Its compset declares on-fault shear/normal directly (40.0 MPa / -120.0 MPa, `user_defined_params.py:134-135`), but its committed, currently-PASSING reference reads 24.68 MPa / 76.89 MPa instead -- the gate is internally self-consistent (numpy/jax match the Fortran reference) but there is NO independent published drv.a6 submission, so nothing has ever checked this case against external truth, and its flip-budget bound was calibrated AGAINST this exact behavior. Do not change drv.a6's reference or bound while this is open -- the gate's meaning is narrower than it looks (self-consistent, not externally validated) until this is resolved.

**Side findings from the investigation, not the main thread:** `test.tpv29` and `test.tpv30` run the SCEC spec's two mutually exclusive initial-condition methods (Part 8): Method 1 and Method 2 respectively. Nothing in either compset currently says so -- worth a comment in both `user_defined_params.py` files at some point (not landed). Pore pressure exists only implicitly, folded into `roumax - rhow` in two expressions, with `eleporep` hardcoded to `0.0d0` and the real formula commented out beside it in the source -- a fragile encoding; the owner's own recollection is that it was added to balance something and the reasoning was lost. Worth flagging as technical debt. `gamar` (over-pressurization ratio, default 0.66) is load-bearing in the stress construction and arrives purely by DEFAULT in `test.drv.a6`, which sets neither `gamar` nor `roumax` itself in its own compset -- an implicit dependency worth naming. `dtau` in `faulting.f90:9` is implicitly `SAVE`d and shared across ALL fault nodes -- a latent bug for `friclaw>=3` cases specifically (rate-and-state), not yet triggered by any currently gated case but worth a pathway row of its own if one doesn't already exist for it.

**Success criterion, if this is ever reopened:** `test.tpv30` at dx=500 must read 27.791 MPa shear / 181.504 MPa normal at `faultst000dp120`, matching tpv29 exactly at the same dx, with no pre-arrival stress relaxation. Independent validation (rule 17 step 6) is already SATISFIED and does not need repeating: `testsys/parity/evidence_tpv30_scec_comparison.py` compares this repo's committed 500m Fortran reference against the owner's own published 100m 2015 SCEC submission and finds physics validity roughly matches (99.2% rupture-extent overlap, ruptured-area ratio 0.978-1.013, median final-slip diff 6.3%, Mw 7.026) -- that is a separate finding from the open port-vs-Fortran defect above and does not resolve it. Full numbers and trace: `NOTES_tpv30_gate.md`. This is the final summary of this investigation; do not append another correction on top of it -- edit this block in place only if new evidence arrives. | 2, 5, 17 | `case_input/test.tpv30/`, `test.reference.results/test.tpv30/`, `NOTES_tpv30_gate.md`, `testsys/parity/evidence_tpv30_scec_comparison.py` | 2026-09-17 | `python3 -c "import testNameList; print('test.tpv30' in testNameList.nameList)"` (expect `False` -- stays unregistered until finding (B) closes); `python3 testsys/parity/evidence_tpv30_scec_comparison.py` (report-only, always exits 0, prints both verdicts); full trace: `git log --oneline -- NOTES_tpv30_gate.md` |
| **P3** | 17 | multi-fault campaign | DEFERRED BY DECISION (owner, relayed 2026-09-16 via the orchestrating session; not independently confirmed beyond that relay): the campaign is left for future work, not pursued now. The input contract is still refused with a named error, so nothing is silently wrong while it waits | nothing -- decision made, not an open question. The design question of how on-fault stations map to faults (`nonfs(i)` is per-fault, `par.st_coor_on_fault` is flat) remains unresolved but is no longer being awaited; it is deferred along with the rest of the campaign |
| **P4** | 7, 9, 10 | the three latent `ntotft>=2` bugs | UNREACHABLE today -- they sit behind item 17's refusal, so they cannot bite | item 17 |
| **P4** | 16(b) | freeze tpv36/37 references | folds into 19(a); listed separately only because the row predates it | see 19(a) |
| **P4** | 11 | -- | CLOSED 2026-09-16 | -- |
| **P4** | 35 | CI: duplicate same-second push-triggered runs | wasted runner minutes, clutters `gh run list`; not a correctness issue | not investigated -- cheap to reproduce next time it happens, not worth budget today |
| **P4** | 19(c) | TPV34 + TPV35 | new builds: no compset, no draft; specs ARE fetched (rule 17 step 1 done). **CLOSED, NOT DEFERRED (owner decision, relayed via coordinator), 2026-09-17:** not being pursued -- this corrects an uncommitted same-day demotion edit in this same file (the owner decision went further than demotion; not a second, independent event) -- still strengthens rather than overturns the net-new-physics finding below; see "blocked on" for the full reasoning and the reopening trigger | nothing, just cost. Rule 17 step 1 DONE (commit `3677581`, on master as of v5.10.0): TPV34/TPV35 specs fetched and recorded in `full_specs.py`, cited by name and part in `NOTES_tpv3435_spec.md` (tracked on master) -- the row's earlier "specs not even fetched" / "step 1, not done" language is stale and corrected here. Rule 17 step 3 also DONE 2026-09-17 (fresh `grep -n "TPV==34\|TPV==35" src/fortran/*.f90`, zero hits): confirmed no Fortran branch exists for either, so this is genuinely new physics, not a port -- `scec_archive/tpv34/{eqdyna3d-v3.2.3-50m-2016,eqdyna3d-v4.0-25m-2016}` and `scec_archive/tpv35/eqdyna3d-v4.1.3-100m-2017` exist (rule 17 step 6 satisfiable eventually). **NEW SCOPING FINDING 2026-09-17 (read-only, no runs; full detail in `NOTES_tpv3435_spec.md`'s new section added this session):** no CVM-H/velocity-model ingestion code exists anywhere in `src/` or `scripts/` (grepped, zero hits) -- TPV34's single largest gap, likely needing a pre-extracted/decimated static grid file per the TPV29 precedent, not a live query library; the existing `nmat>1`/`n2mat==4` material layering (`src/fortran/meshgen.f90:174-201`, mirrored in `src/python/eqdyna/meshgen.py`) is DEPTH-ONLY and does not fit TPV35's per-side-of-fault 1D profile as-is; `swtwNucleation` (`src/fortran/faulting.f90:386-421`) looks structurally reusable for TPV34's shear-stress-bump nucleation, but TPV35's externally-supplied lowered-yield-stress patch has no precedent in the from-scratch dynamic path (only the unrelated mode=2 cycle-restart netcdf path reads external per-node state); no real-recorded-waveform comparison infrastructure exists for TPV35's validation angle, but per the TPV29 precedent this can defer to a post-gate evidence script, not a gating blocker. Net read: TPV35 closer on material, TPV34 closer on nucleation; TPV34's velocity-model gap is the single biggest net-new subsystem of either. **CLOSURE REASONING 2026-09-17 (owner decision, relayed via coordinator):** the owner DID publish both TPV34 and TPV35 historically -- confirmed in `scec_archive/tpv34/{eqdyna3d-v3.2.3-50m-2016,eqdyna3d-v4.0-25m-2016}` and `scec_archive/tpv35/eqdyna3d-v4.1.3-100m-2017` -- so a validation baseline exists (rule 17 step 6 satisfiable). BUT the implementation that produced those runs predates this repository and is not recoverable from it: this repo's first commit is 2018-11-26 (a squashed import carrying a 2014 author date in its message) and its oldest tag is v5.2.0 (2020-10-07); `git log --all -S"TPV==34"` and `-S"TPV==35"` return nothing but this campaign's own recent commits, and `src/fortran/faulting.f90:405` still dispatches only `TPV==201 .or. TPV==36 .or. TPV==37 .or. TPV==29`. So this is confirmed new physics plus a new subsystem on both Fortran and Python for two cases, not a revival with a recoverable reference implementation -- the "net-new physics" finding above stands and is now stronger, not weaker. **Reopening trigger (owner-only -- not a task for the next session to go searching for):** the owner may still hold the 2016/2017 v3.2.3/v4.0/v4.1.3 sources elsewhere (another repo, a TACC scratch, a backup). If that source surfaces, TPV34/35 change from "build a velocity-model-ingestion subsystem from the spec" to "port known-working physics with the submissions already in hand" -- a completely different, much smaller job and grounds to reopen. Already put to the owner directly; not a next-session action. |

Two standing notes that are not items:

* **`writeCompTime` is read from nowhere** (`globalvar.f90:109`), so every
  per-stage timing number is invisible by default. That is how a 511x error in
  `compTimeInSeconds(2)` survived. Giving it an input slot is a case-input
  contract change; not queued, but it is why timing bugs hide here.
* **Four claims in this file did not survive measurement** (24(a)'s half
  tractions, a 33% JAX regression, a friclaw-4 coverage gap, a
  compile-fraction figure) and two nearly became fixes. Prefer measuring a
  claim over repairing it.

| # | Item | Rule | Surface | Re-check interval | Last checked | Command |
|---|---|---|---|---|---|---|
| 1 | ~~Untracked build artifacts~~ resolved by `.gitignore` update (8c40871); recheck each release | 12 | repo root, `src/` | every release | 2026-09-09 | `git status --porcelain` |
| 3 | ~~`check.test.py` swallows `AssertionError` ... never sets a non-zero exit~~ resolved (34691db): loop moved into `main()`, every comparison result accumulated, `sys.exit(1)` on any `FAIL`, missing file, or zero comparisons run; guarded by `testsys/regression/test_check_gate_fails_on_mismatch.py` | 2, 3 | `check.test.py` | each change to test infra | 2026-09-09 | `python3 check.test.py; echo exit=$?` |
| 4 | ~~`compare_nc_files` call commented out~~ resolved (34691db): call uncommented, `compare_nc_files` is a pure function with no import-time side effects, exercised directly in `testsys/unit/test_check_comparisons.py` and via the sandboxed `fault.dyna.r.nc` case in the regression guard above | 3 | `check.test.py` | each change to test infra | 2026-09-09 | `grep -n compare_nc_files check.test.py` |
| 5 | ~~`testAll.py` ran `rm -rf test` unconditionally~~ resolved (f847b3b): `testAll.py` is now a thin wrapper delegating to `testsys/e2e/run_e2e.py`, which renames the previous `test/` to `test.prev/` (one level of history) instead of deleting it | 8 | `testsys/e2e/run_e2e.py` | each change to test infra | 2026-09-09 | `grep -n "shutil.move(test_dir, prev_dir)" testsys/e2e/run_e2e.py` |
| 6 | ~~README Installation section instructed a recursive `chmod -R 755 install-eqdyna.sh scripts`~~ resolved (this release): `install-eqdyna.sh` itself no longer bulk-chmods `scripts/` (was doing so on every build, rule 13), so README now only needs `chmod 755 install-eqdyna.sh` | 13 | `README.md`, `install-eqdyna.sh` | each release | 2026-09-09 | `grep -n "chmod -R" README.md install-eqdyna.sh` |
| 41 | TO-DO, low priority: `driver.py:182` reads `xp.asarray(inv['stress_i0'])` with no defensive `.copy()` -- for the numpy backend `np.asarray` on an existing ndarray returns the SAME object, so the time loop's carry state is ALIASED to, not copied from, `inv['stress_i0']`. Currently HARMLESS: `inv['stress_i0']` is built once (`assembleGlobalKU.py:205/209/229`) and read exactly once, at this line -- nothing else in the shipped codebase re-reads it, so the carry's in-place mutation (`assembleGlobalKU.py:261`, deliberate for performance) cannot corrupt anything today. Found as a byproduct of the now-rejected TPV30 Method-1 investigation (see item 19(b)/39, unrelated in substance): that draft needed a SECOND read of `inv['stress_i0']` later in the per-step assembly to implement "subtract initial" per spec, and that second read observed the already-mutated aliased array, causing `test.tpv8 x python-numpy` to fail its bound by ~1.2e17x in that investigation's own fresh sweep -- how the gap was found. Suggested fix: one-line defensive `.copy()` at `driver.py:182` (`xp.asarray(inv['stress_i0'].copy())` or equivalent); provably a no-op for all current usage, not landed this session since it surfaced as a side effect of rejected work and has not been verified against a fresh full gate | 2 | `src/python/eqdyna/driver.py:182` | before any future change adds a second read of `inv['stress_i0']` | blank | `python3 testsys/run.py all` (confirm no-op); `grep -c "stress_i0" src/python/eqdyna/*.py` (expect 4: 3 in `assembleGlobalKU.py`, 1 in `driver.py`) |

| 7 | TO-DO: `netcdf_read_on_fault_eqdyna` hardcodes fault index `1` instead of loop var `ift` in all `fric(...)` assignments, so on-fault netcdf input is never applied to faults 2+ (fault-N data overwrites fault 1); `ii,jj` grid indices are also unchecked against bounds for faults 2+ (built from fault-1 extent). The restart routine below it (`netcdf_io.f90:159-179`) shows the correct `ift` pattern. Bites only `ntotft >= 2` — no current test case exercises it; fix needs a multi-fault regression case per rule 10. Does NOT explain the tpv1053d reference mismatch (that case runs `ntotft=1`; its golden reference is stale, carrying an uninitialized denormal). | 2, 10 | `src/fortran/netcdf_io.f90:76-105` | until fixed | 2026-09-09 | `grep -n "i, 1) = on_fault_vars" src/fortran/netcdf_io.f90` |

| 8 | ~~PML region-14 cross-axis bug~~ FIXED a844e92 (2026-09-13): y now vs ymax in all 4 sites; z==zmin2 gap closed. Impact was real: drv.a6 tstk moved up to 29 MPa; refs regenerated; guard test_pml_region_axes.py added | 2, 10 | `src/comdampv.f90`, `src/assembleGlobalKU.f90` | guarded | 2026-09-13 | `python3 testsys/regression/test_pml_region_axes.py` |
| 9 | BUG (latent, ntotft>=2): `output_onfault_st` opens unit 51 only when j==1 but writes unconditionally | 2 | `src/fortran/library_output.f90:29,37` | until fixed | 2026-09-12 | `grep -n "if(j==1)" src/fortran/library_output.f90` |
| 10 | BUG (latent, ntotft>=2): `MPI4arn` allocates fltl..fltu from previous fault's counts before zeroing fltnum; re-allocation aborts on fault 2 | 2 | `src/fortran/meshgen.f90:220-227` | until fixed | 2026-09-12 | `grep -n "allocate(flt" src/fortran/meshgen.f90` |
| 11 | ~~CHECK: `mesh4num` on-fault tolerance vs `checkIsOnFault` may diverge~~ RESOLVED BY INSPECTION (2026-09-16): the two passes CANNOT disagree, for two independent reasons. (1) STRUCTURAL: both call the same `checkIsOnFault` (`src/fortran/meshgen.f90:750`) with the same argument -- `countMeshEntities.f90:46` for the count, `createMasterNode` at `meshgen.f90:784` for the allocation -- and both pass the RAW GRID y. `insertFaultInterface` (`func_lib.f90:37-40`) writes the rough-fault y-blend to its `ycoort` output and never touches `nodeCoor(2)`; the blend reaches only `meshCoor(2,.)` (`meshgen.f90:72,805`). That separation is load-bearing, not incidental: `checkIsOnFault` tests `nodeCoor(2)==0.0d0` by EXACT float equality, which a blended y would break, so keeping the blend out of `nodeCoor` is what makes the exact comparison sound. Anyone tempted to 'simplify' by blending in place would reintroduce this item as a real bug. (2) ENFORCED ANYWAY: `meshGenError` (`meshgen.f90:403-409`, called at `:113`) compares `nftnd0` against `nftnd` per fault and calls `abortRun(ERR_MESH_FAULT_MISMATCH)` = exit 44 on any disagreement. So a divergence is a hard stop, not silent mesh corruption -- rule 2 satisfied without a new guard. NOTE: this row pointed at `mesh4num.f90` until 2026-09-16; that file is `countMeshEntities.f90`, and the earlier audit's claim that the allocating call is at `meshgen.f90:94,98` was also wrong -- those two are inside `if (C_degen > 3.0d0)` and tag wedge elements, not fault nodes. | 3 | `src/fortran/countMeshEntities.f90:46`, `src/fortran/meshgen.f90:750,784,403-409` | resolved 2026-09-16 | `grep -n 'nftnd0(i)/=nftnd(i)' src/fortran/meshgen.f90` |

| 12 | ~~fric_tp_h=0 TP bug~~ FIXED (2026-09-13): thermop kernels now use per-node fric(40) (=0.02 m, TPV105-3D spec h=20mm). Verified against clean-room v5.2.0 (the SCEC-verified tag): fixed==v5.2.0, h=0 categorically different (~1 s rupture time, ~2.9 m slip). tpv1053d reference regenerated. Was: `fric_tp_h` (TP shear-zone half-width, denominator in every thermop.f90 kernel) is never assigned in src/ — runs as 0.0 (confirmed by compiled probe). Intended 0.02 is written to on_fault_vars slot 40 (fric(40)) by case.setup but never wired to the global thermop reads. All TP results from this tree used h=0. Fix is gate-moving (tpv1053d reference regen) — needs owner decision | 2 | `src/globalvar.f90` (fric_tp_h), `src/thermop.f90`, `src/netcdf_io.f90` (slot 40) | until fixed | 2026-09-13 | `grep -rn "fric_tp_h" src/` |

| 13 | ~~PML boundary inclusivity~~ RESOLVED (2026-09-13): both callers standardized to inclusive >=/<= (an on-bound point classifies with zero damping distance, harmless), and a mesh-time guard checkPMLAlignment stops loudly if any element center lies exactly on a PML bound (the cascade's remaining geometric assumption, now enforced not assumed). Bit-gate confirmed no output change. fric slot 6 retired e90ca03 (dead 'surface pore pressure' reads removed) | 2 | `src/func_lib.f90` | guarded | 2026-09-13 | `grep -n checkPMLAlignment src/meshgen.f90` |

| 14 | ~~parity reproducibility~~ RESOLVED (2026-09-13): testsys parity + perf tiers landed — pydump link-time seam (default binary byte-neutral, now a regression check `run.py parity`'s functional-neutrality step), make_fixtures.py regenerates oracles, `run.py parity` gates NumPy+JAX vs Fortran at documented thresholds; `run.py perf` is pinned single-core and ratio-guarded vs baseline.json. Pinned verdict: JAX-CPU 0.61x Fortran wall-clock (1.6x faster), NumPy 1.8x slower. Was: `python/README-parity.md`'s parity numbers (NumPy/JAX ports vs Fortran, JAX-CPU 1.1-2.6x) were established against instrumented spike builds — `src/pydump.f90` (the oracle-dump instrumentation) is not in tracked `src/` or wired into `src/makefile`, `python/tests/data/` is missing the `pydump_*.txt` fixtures `run_parity.py` needs, and no tier under `testsys/` exercises `python/` at all. Running `run_parity.py` against the current tree fails with `FileNotFoundError: pydump_header.txt`. Not a correctness finding against the physics fixes in v5.3.6 — the port work and its parity claim are real, just not reproducible in-tree yet. Needs an owner decision: land `pydump.f90` + fixtures + a `testsys/` tier, or keep documenting the parity claim as spike-only in `python/README-parity.md`. | 2, 4, 10 | `python/`, `src/pydump.f90` (absent), `testsys/` | until decided | 2026-09-13 | `python3 python/run_parity.py tests/data/tpv8_serial 114 tests/data/tpv8_serial_frt.txt0.golden` |
| 15 | ~~standalone-port acceptance numbers only in a commit message~~ RESOLVED (2026-09-13, v5.4.0): `testsys/run.py accept` reproduces the standalone-Python (zero-Fortran) acceptance check for tpv8/tpv104/tpv1053d against committed `test.reference.results/` at rule-5's 1e-3 threshold. Fresh numbers this release: tpv8 max_abs_diff=8.231401e-11, tpv104 3.388109e-07, tpv1053d 3.358889e-06 — all `SUCCESS`. Was: item flagged during the v5.3.7-audit that `057e589`'s 8.2e-11/3.4e-7/6.7e-6 figures existed only in a commit message with no committed test to reproduce them (rule 2/4 gap); closed by landing the `accept` tier rather than by documenting the gap. | 2, 4 | `testsys/parity/test_standalone_acceptance.py` | guarded | 2026-09-13 | `python3 testsys/run.py e2e` (the `accept` tier was folded into the sweep 2026-09-15) |

| 16 | QUEUED: revive dormant compsets into the gate — (a) add test.meng2023a / test.meng2023cb to testNameList.py (golden references already exist in test.reference.results/ but are ungated); (b) freeze tpv36/tpv37 references from a fresh verified run (SCEC-verified v5.3.3-era, exercise C_degen wedge degeneration which no gated case covers). Note: the fdc compsets (liu2020.fdc.*, bp1001.fdc.*) are production EQsimu cycle configs (EQdyna mode=2 inside the cycle loop), NOT dormant tests — revival for them means a gated mode-2 restart case (currently zero gate coverage of the restart path), not a standalone dynamic run. Sequenced after the standalone-5/5 extension and v5.4.0 re-cut | 3, 10 | `testNameList.py`, `test.reference.results/` | queued 2026-09-13 | `grep -c meng testNameList.py` |

| 17 | DEFERRED BY DECISION (owner, relayed 2026-09-16 via the orchestrating session; not independently confirmed beyond that relay): the multi-fault support campaign is left for future work, not pursued now. **PREREQUISITE FOUND 2026-09-16, and it is not one of the four known bugs:** the INPUT CONTRACT is broken on the writer side. `scripts/case.setup` writes ONE on-fault station count on line 2 of `bStations.txt`, while `readInputFiles.f90:152` reads `(nonfs(i), i = 1, ntotft)` -- ntotft of them. They agree only at ntotft==1. Measured on a two-fault test.tpv8: the list-directed read runs off the short line into the station coordinates and the run dies with `Bad integer for item 2 in list input`, **exit 2**, a raw gfortran I/O error naming neither ntotft nor the cause. That crash also means the DELIBERATE multi-fault stop at `meshgen.f90:790` (`ERR_MESH_MULTIFAULT_MSNODE`, exit 45) is UNREACHABLE through the normal path -- the input read fails first, so a guard that exists has never once fired. Nobody has run ntotft>=2 through `case.setup` successfully, which is why items 7/9/10 have stayed latent: they sit BEHIND this. Writing ntotft copies of the one count would silence the crash and be wrong -- `nonfs(i)` is the count FOR FAULT i and `par.st_coor_on_fault` is a flat list with no per-fault assignment, so that assignment is the first real design decision of this campaign. `case.setup` now REFUSES ntotft>1 with a message naming ntotft, the file, the reader line and this item (rule 2), guarded by `testsys/regression/test_multifault_refused.py`, which also pins that ntotft==1 still works so the refusal cannot be satisfied by refusing everything. THEN the four known bugs: item 7 netcdf_io hardcoded fault index; item 9 output_onfault_st unit-51; item 10 MPI4arn stale-count allocation; item 11 (CLOSED 2026-09-16, cannot diverge). Design reference: EQquasi's step-over (bp1002) implementation at /home/staff/dliu/0.Dunyu/EQquasi, manuscripts 20260812_eqquasi_stepover. Ship with a two-fault step-over regression case + frozen reference (rule 10). Coupling note: fric slot 6 (pore pressure) is dormant-zero in BOTH codes -- if EQquasi re-enables it, EQdyna's retired slot-6 reads (e90ca03) must be restored. | 2, 10 | `scripts/case.setup`, `src/fortran/readInputFiles.f90:152`, `src/fortran/netcdf_io.f90`, `src/fortran/library_output.f90`, `src/fortran/meshgen.f90` | deferred 2026-09-16 | `python3 testsys/regression/test_multifault_refused.py` |

| 18 | RESOLVED (M10, 2026-09-14): standalone-port drv.a6 (C_elastic==0/plastic, friclaw==4) -- ported Drucker-Prager viscoplasticity (calcElemKU.f90:127-161), gravity body force (assembleGlobalKU.f90:16), lithostatic pre-stress init (meshgen.f90:103) into kernels_numpy.py/kernels_jax.py/main.py, gated so C_elastic==1 cases (tpv8/104/1053d/10) are PROVABLY unaffected (accept tier 4/4 green, parity tier green throughout). Checkpoint-verified BIT-EXACT against a fresh src/eqdyna build on the same serial mesh: initial per-element stressArr (interior+PML) and step-1 hypocenter Tn/Ts/Td. Found+fixed a real bug: faulting.f90's rsfNucleation TPV==2802 branch (one-time nt==1 RSF-state/theta_pc re-derivation) was missing from port_rsf.py/port_rsf_jax.py -- without it rupture never nucleates. Fix improved full-run max_abs_diff 6.69e8 -> 7.27e7 and matched hypocenter cumulative slip to step 50/120. The remaining 8% fault-node rupture-arrival disagreement was root-caused by a fresh three-way experiment (a freshly-built serial src/eqdyna vs the committed 4-rank reference vs the standalone port, all on the same serial case, fnft-column existence-flip + timing-shift accounting): A (serial Fortran vs 4-rank ref, decomposition-only change) = 372/5151 flips; B (standalone vs that same serial Fortran) = 439/5151; C (standalone vs 4-rank ref) = 391/5151 -- all the same order of magnitude, A∩B=212/372, A∪B explains 388/391 of C. A alone proves the 4-rank reference is unreachable by ANY serial run (genuine decomposition/reduction-order-sensitive rupture-front chaos on this fractal-rough/viscoplastic/longer-duration case, consistent with the documented PML-fix episode's 43-node flip). Median |Delta fnft| among ruptured-in-both nodes is 0.0417s in ALL THREE pairs -- bulk agreement is real, flips are a genuinely bistable minority. test.drv.a6 is now IN test_standalone_acceptance.py's CASES (tier is 5/5 GREEN) with a case-specific two-part gate (drv_a6_gate(), not the scalar PER_CASE_ABS_BOUND path): median|Delta fnft|<=0.1s AND matched-arrival physics-column max diff<=1.1e9 (~30x measured 3.996e7) AND total existence-flips+timing-shifts<=450 (measured C=391, under budget). Full derivation + all three-way numbers are in that file's module docstring "Milestone 10 CLOSURE" section | 2, 4, 5 | `python/eqdyna/kernels_numpy.py`, `kernels_jax.py`, `port_rsf.py`, `port_rsf_jax.py`, `standalone/main.py`, `standalone/meshgen.py`, `testsys/parity/test_standalone_acceptance.py` | resolved 2026-09-14 | `python3 testsys/run.py e2e` (the `accept` tier was folded into the sweep 2026-09-15) (5/5) |

| 20 | ~~TPV29 spec-resolution run~~ CLOSED, NOT DEFERRED (owner decision, 2026-09-16): **the 50 m run will not be done.** What exists and stays: dx=500 gated in the sweep on all three backends with a frozen reference (item 25); dx=100 RUN and cross-code validated against EQdyna's 2015 SCEC submission -- rupture time 0.006 s median, final slip 0.22% median over 24 on-fault stations, ruptured area 749-756 km2 (naive node-count rule; item 28) vs the cvws ~746 plateau, Mw ~~7.45~~ **7.034 (corrected 2026-09-16, item 28: the original 7.45 had no reproducible provenance and is physically impossible for this run's own moment by 4.7x)**; dx=500/200/100 converge (0.076 / 0.028 / 0.006 s median). The 100 m run IS the cross-code validation; 50 m would only have tightened a number already converged, at 2-3.5 h on 16 ranks. The geometry stays shipped at both 100 m and 50 m (`bFault_Rough_Geometry.tpv29.50m.txt`, 14 MB) and `testsys/e2e/full_specs.py` keeps its FULL_SPECS entry, so anyone who wants the run can start it -- nothing was removed, it is simply not queued. The remaining gap is item 28 and it is about REPRODUCIBILITY, not doubt: those validation numbers are prose-only and the comparison baseline `scec_archive/tpv29/` is gitignored and external, so no script can reproduce them from a clean clone. Fix that before the numbers go in a paper. | 3, 4 | `case_input/test.tpv29/`, `testsys/e2e/full_specs.py` | closed 2026-09-16 | `python3 testsys/e2e/run_e2e.py --cases test.tpv29` |

| 21 | ~~item 18's drv.a6 chaos-aware criterion had two supporting inputs (A/B flip counts behind the 450 bound; the gpu-tier's cited wall-clock comparison) that were docstring/commit-message-only, not reproducible from a committed script~~ resolved (this session, SHA cff31de): `testsys/parity/evidence_drv_a6_chaos.py` regenerates all of it from scratch, report-only (never a gate, never wired into `testsys/run.py`), importing (not duplicating, rule 1) `load_coordinate_aligned`/`align_two_frt_files`/`flip_decomposition`/`drv_a6_gate` from `test_standalone_acceptance.py` (which was refactored to expose those as reusable functions without changing its own pass/fail behavior). Fresh run this session: A=372/450 (153 ref-only+176 py-only+43 timing-shifts, ruptured-both=1075), B=439/450 (193+200+46, ruptured-both=1058), C=391/450 (160+190+41, ruptured-both=1068), A∩B=212, A∪B=599, median|Δfnft|=0.0417s in all three — BIT-IDENTICAL to item 18's originally recorded 372/439/391/212/599/0.0417s, because the serial single-rank/single-thread pipeline (fixed compiler+hardware) is fully deterministic; the documented "chaos" is cross-configuration (rank-count / reduction-order) sensitivity, not run-to-run noise, so exact reproduction is the expected, stronger result here, not license to tighten `DRV_A6_TOTAL_FLIP_BOUND`. GPU-vs-CPU timing (`--gpu-only`): 35.17s GPU vs 57.01s CPU (1.62x) vs the cited 33.7s/46.9s (1.39x) — same order of magnitude; GPUs were at 82-100% utilization from other tenants at start (nvidia-smi contention caveat printed by the script), so an exact match was never expected or required. One-line pointer added to `test_standalone_acceptance.py`'s "Milestone 10 CLOSURE" docstring. This item's separate observation about a missing Drucker-Prager kernel-level unit test is NOT addressed by this closure — carried forward as item 22. | 2, 4, 6, 10 | `testsys/parity/evidence_drv_a6_chaos.py`, `testsys/parity/test_standalone_acceptance.py` | each release | 2026-09-14 | `python3 testsys/parity/evidence_drv_a6_chaos.py --gpu` |

| 23 | ~~TO-DO (carried forward from item 21's release-audit finding, 2026-09-14): M10 Drucker-Prager viscoplasticity had zero unit/parity-level test isolating the kernel itself~~ RESOLVED (`d5da43e`, 2026-09-16): `testsys/regression/test_drucker_prager_kernel.py` compiles a driver against the real unmodified `calcElemKU.f90:127-161` with plain gfortran (no MPI, no case, no mesh) and diffs it against the Python port's `assembleGlobalKU._drucker_prager` on 4 synthetic stress states (elastic, yielding, near-yield-boundary, shear-dominated), isolating the block by input construction (`vl=dl=0, C_Q=0`) not extraction. Picked up by `testsys/run.py regression`'s existing glob, zero run.py changes. Verified independently twice: once at landing, once fresh this session | 2, 10 | `src/fortran/calcElemKU.f90:127-161`, `testsys/regression/test_drucker_prager_kernel.py` | resolved 2026-09-16 | `python3 testsys/regression/test_drucker_prager_kernel.py` |
| 19 | QUEUED (owner-named 2026-09-16): **revive TPV30, 34, 35, 36, 37 into the gate.** Follow PROJECT_RULES rule 17 / CLAUDE.md; these are THREE different amounts of work and should not be planned as one. **(a) TPV36 + TPV37 -- cheapest, do first.** Compsets already exist (`case_input/test.tpv36`, `test.tpv37`), dx=500, term=6, friclaw=1, and `faulting.f90` already has their `TPV==36`/`TPV==37` branches in `swtwNucleation`'s smoothed forced-rupture list. They need only a verified run + one frozen `frt.canonical.txt` each + registration in `testNameList.py` and `matrix.py`. **Real coverage value:** both set `par.C_degen = par.dip` (wedge degeneration, `test.tpv36/user_defined_params.py:46`) and **no gated case exercises C_degen at all** -- verified 2026-09-16 across all 8. That code path ships untested today. **RESOLVED 2026-09-17: (a) is now fully CLOSED, both TPV36 (v5.9.0) and TPV37 (v5.10.0) gated on all three backends -- `C_degen` is exercised by two gated cases now, see item 19(a)'s own row for the full account.** **(b) TPV30 -- promotion attempted 2026-09-17 (mira-volkov), IN PROGRESS, real divergence found, gating BLOCKED on root-causing it (rule 17 steps 4/7 not satisfied), not a stall.** Prerequisite 24(b)-(f) closed 2026-09-17, so the attempt went ahead: `TPV==30` branch landed in both `faulting.f90`/`faulting.py`, `case_input/test.tpv30/` compset and a verified Fortran-only reference exist but the case is NOT registered in `testNameList.py`/`matrix.py`. A full 3-backend sweep found python-numpy and python-jax agreeing with each other to ~1e-3 while both disagree with the Fortran reference by up to 4.0e8 Pa at 3310/3321 nodes -- a real, deterministic port divergence, binary-search-localized to somewhere between t=1s (bit-exact on all three) and t=6s (already flipped). Full finding, evidence script, and next steps are in item 39's own row (priority table) and `NOTES_tpv30_gate.md`; not duplicated here. **UPDATE 2026-09-17 (`f110c75`):** rule 17 step 6 (independent validation) now satisfied -- physics validity ROUGHLY MATCHES the owner's own 100m 2015 SCEC submission (99.2% rupture-extent overlap, area ratio 0.978/1.013, median slip diff 6.3%); the still-unresolved port-correctness divergence (numpy/jax vs Fortran, up to 30% at t=20s) is untouched by this and remains the sole blocker on rule 17 steps 4/7. Full account: item 39's own row. **(c) TPV34 + TPV35 -- new builds, most work.** Nothing exists yet: no compset, no scratch draft, no reference. Rule 17 step 1 DONE 2026-09-17 (commit `3677581`, on master as of v5.10.0): specs fetched and cited by name/part in `NOTES_tpv3435_spec.md` (tracked on master); `full_specs.py` carries both entries now. Check `faulting.f90` for a `TPV==34`/`TPV==35` branch before trusting any run -- TPV29 taught that a missing branch is silent and a case may be made to impersonate another TPV to hide it (rule 17 step 3 confirms neither branch exists yet). **(c) TPV34+TPV35 -- CLOSED 2026-09-17 (owner decision), see item 19(c)'s own row for full reasoning.** Also still here from before: `TPV2802_50_a6` is an old-format compset (FE_*.txt, no `user_defined_params.py`) and ungated; `test.drv.a6.v2` (tpv=2802, friclaw=4, term=25) is ungated; `TPV==2800/2801` branches in faulting.f90 have no surviving configs -- recover from history or delete the dead branches (rule 1). The `fdc` compsets (liu2020.*, bp1001.*) are production EQsimu cycle configs, NOT dormant tests. **SUITE-COST DECISION 2026-09-16 (owner, relayed):** a working TPV goes in the DEFAULT sweep, not an opt-in tier -- applies to tpv36/37 now and tpv30/34/35 as each proves out. Not independently confirmed beyond the relay; recorded as the operating policy going forward | 1, 3, 10 | `case_input/test.tpv36`, `test.tpv37`, `scratch/tpv30/`, `src/fortran/faulting.f90` | queued 2026-09-16 | `ls case_input/ test.reference.results/` |

| 22 | ~~station filename overflow~~ FIXED (2026-09-14): sign-aware names, verified on TPV29 trial. Remaining from this row: SCEC normal-stress sign convention check before any cvws upload. Was: library_output.f90:29 `(i3.3)` overflow for negative-x stations — half of TPV29's spec station list writes to colliding `faultst***dpXXX.txt` names (already visible in old drv.a6 outputs). Needs sign-aware formatting before TPV29 station output is usable. Related: on-fault normal-stress sign convention differs from SCEC (correctSCECStOutputFormat.py exists; verify before any cvws upload) | 2 | `src/library_output.f90:29` | until fixed | 2026-09-14 | `ls test/*/faultst\*\*\** 2>/dev/null` |

| 24 | (a) RESOLVED BY MEASUREMENT (2026-09-16) -- **the half-traction claim does not reproduce.** Measured on test.drv.a6 (`C_elastic=0`, friclaw 4), 4,747 fault nodes deeper than 2 km, actual step-1 on-fault traction against `setPlasticStress`'s own formula (`strVert = -(roumax - rhow*(gamar+1))*depth*grav`, and at `str1ToFaultAngle=45` deg `cos(2t)=0` so Syy=strVert is the fault normal and Sxy=devStr the strike shear): **Tn ratio median 1.0018** (intended -1.3370e+08, actual -1.3377e+08), **Ts ratio median 0.9858**. The p10-p90 spread 0.93-1.08 is the fractal roughness perturbing the local fault normal, not a systematic factor. Probe: scratchpad/probe24b.py, reproducible from a serial drv.a6 case in ~2 min. MOST LIKELY 24(a) WAS item 26's `arn` doubling, fixed 2026-09-14: the mechanism matches exactly -- `totalMass = (massSlave+massMaster)*arn` in `faulting.f90:113-123`, so a doubled arn halves every traction, depth-independently. If so, item 26's claim that its changed path is 'provably never entered' by the gated compsets is WRONG for drv.a6, and that claim should be re-checked rather than trusted. **CORRECTED 2026-09-16 (owner-verified): that attribution is WRONG -- 24(a) was MIS-SCOPED, not explained by item 26.** `test.drv.a6` has a deliberately asymmetric y-domain (`ymin,ymax = -31/+30 km`, `ny=2`) putting the rank boundary at `y=-500 m` while the fault sits at `y=0` -- one grid cell off -- and it uses `insertFaultType=2` with `fymin=fymax=0`, i.e. the roughness is morphed into the physical y, so the case is topologically a PLANAR fault with no real nominal-grid y-extent, the precondition for entering `MPI4arn`'s add-back path at all. drv.a6 therefore never entered that path, either before or after item 26's fix, and item 26's arn-doubling bug could not have produced -- or explained the disappearance of -- 24(a)'s half-traction result. Item 26's 'provably never entered' claim in fact HOLDS for drv.a6 (for the structural reason above, not the domain asymmetry) but FAILS for tpv36/tpv37, which enter that path immediately (dipping faults, `C_degen>3` giving real nominal-grid y-extent) -- see item 26's row and `testsys/regression/test_dipping_fault_y_split.py`. Also worth recording: BEFORE item 26 landed, drv.a6 was protected from that exact bug ONLY by its 1 km y-domain asymmetry, not by anything structural -- a 'tidier' symmetric `-30/+30` domain would have put the rank boundary exactly on the fault and halved every traction, hitting the bug by luck rather than design. Do not tidy that domain. Consequence for item 32: the half-traction explanation for drv.a6's decomposition sensitivity is ELIMINATED -- the case runs with correct tractions, so the 329-flip serial-vs-4-rank floor is genuinely about decomposition. (b) RESOLVED 2026-09-17: Tv has a real input slot. `readInputFiles.f90`'s `tv = 2*dz/NUC_VS_FIXED` is gone; `tv` is read from `bGlobal.txt` (new viscoplastic block at the end of the file, written unconditionally by `case.setup`), and the Python port reads the same record instead of recomputing it. `par.viscoplasticRelaxTime = None` resolves to the old `2*dz/3464` in `lib.resolveViscoplasticParams`, so an unset case is bit-for-bit unchanged; TPV30 sets 0.05 s directly. A `bGlobal.txt` without the block is refused loudly on both sides (ERR_INPUT_FILE_STALE = 22), never defaulted. (c) RESOLVED 2026-09-17: `func_lib.f90`'s new `devStrDepthTaper` is SCEC TPV29/30's Omega(depth) and multiplies `setPlasticStress`'s `devStr`; depths come from `par.devStrTaperDepthStart/End` (both unset = no taper, exactly 1.0d0, so the multiply is an IEEE identity; a half-configured pair is refused). Ported to `func_lib.py`/`eqdyna3d.py` and gated by `testsys/regression/test_dev_str_depth_taper.py`, which compiles the REAL `devStrDepthTaper` and checks it against the spec's closed form AND against the port bitwise, at 12 depths, in both configurations (measured: max |fortran - spec| = 0.000e+00, fortran == port bitwise, Omega(19500 m) == 0.5 exactly). (d) ~~undocumented +7.3215 constant in setPlasticStress's depth argument (`meshgen.f90:104`)~~ CLOSED 2026-09-16 (owner decision, relayed): "do what fortran do" -- kept VERBATIM on both Fortran and Python sides, as a deliberate bit-for-bit parity choice. Provenance stays unknown; this is not a thing to derive, justify, or change, and is not re-opened as a question. Still load-bearing as measured (the probe above had to adopt it to get the 1.0018 traction ratio); (e) RESOLVED 2026-09-17: `loadFrtData` calls the new `lib.shearModulusFromPar(par, depth)` -- rho*Vs^2 from the case's OWN material, layer-selected for `nmat>1` with `meshgen.f90:187-198`'s own comparisons, raising below the deepest layer rather than substituting a constant. `magnitude` reaches only a plot title, not `fault.dyna.r.nc`, so no gated artifact moved. The `test_lib.py` fixture had to grow a material, which is rule 2's "when you remove a fallback, the tests that covered it fail" in action. (f) RESOLVED 2026-09-17: the plastic-strain output window is `par.plasticOutputHalfWidth`, defaulting to the same (5, 2, 8) km box `library_output.f90` hardcoded, so no gated case's `pstr.txt*` changes. NOTE the Python port does not implement `output_plastic_strain` at all (pre-existing gap): it reads the window and does not use it. LEFTOVER worth one line: `scripts/fractal_stress_diamond_square/tpv104.200m.asp.ref/` is a frozen snapshot with its own `case.setup` and `bGlobal.txt`, so its input file now predates the solver and the current binary refuses it loudly; its own `case.setup` cannot regenerate it. | 2, 5, 10 | `src/fortran/faulting.f90`, `src/fortran/meshgen.f90`, `src/fortran/readInputFiles.f90`, `src/fortran/func_lib.f90`, `src/fortran/library_output.f90`, `scripts/lib.py`, `scripts/case.setup`, `scripts/defaultParameters.py`, `src/python/eqdyna/{readInputFiles,func_lib,eqdyna3d}.py` | (b)(c)(e)(f) resolved 2026-09-17, (d) closed 2026-09-16 | `python3 testsys/regression/test_dev_str_depth_taper.py`; `python3 testsys/e2e/run_e2e.py --cases test.tpv29,test.drv.a6 --backends fortran,python-numpy,python-jax` |

| 25 | ~~revive TPV29 as a gated case~~ RESOLVED (v5.5.0, 2026-09-14): `case_input/test.tpv29` gated at dx=500 (fast tier, 4 ranks, decomp (2,1,2), ny=1 keeps the fault off MPI boundaries), frozen reference in `test.reference.results/test.tpv29/`, `testsys/e2e/full_specs.py` FULL_SPECS entry at the spec-preferred dx=50/term=20, compset README with inline reference figure. Confirmed reproducible: this release's `python3 testsys/run.py all` (8/8 e2e cases, src/eqdyna build) matched the frozen reference bit-for-bit. HPC 50 m bundle lives in `scratch/tpv29/hpc50m/` (not tracked; scratch/ is ephemeral per project convention) | 3, 10 | `case_input/test.tpv29/`, `test.reference.results/test.tpv29/` | guarded | 2026-09-14 | `python3 testsys/run.py all` |
| 26 | ~~fault-plane/MPI-boundary 0.5x traction halving~~ FIXED (2026-09-14): root cause was `MPI4arn` (`src/meshgen.f90`) summing `arn` across an MPI-partition boundary in a fault-normal direction (zero nominal fault extent, per `fltxyz` -- always y, EQdyna faults are x-z-plane) as though it divided the fault surface, when it actually duplicated it (every rank touching that boundary already built the full local fault-node grid). Direct dumps (`scratch/mira_faultmpi_guard`): arn at an identical physical fault node = A serial/x-split/z-split, 2A under a y-splitting decomposition (1,2,1) -- and `fnms` (the OTHER quantity on the same `fltMPI`-gated exchange, `assembleGlobalMass.f90` `addFaultBoundaryTerm`) confirmed bit-identical across the same decompositions, i.e. NOT affected, no change needed there. Fix: `syncArnBoundary` now skips arn's cross-rank add-back (only) when `fltxyz(2,dimId,ift)==fltxyz(1,dimId,ift)` for that boundary's direction, while still setting `fltMPI(k)` and still exchanging data so `fnms`/`nodalMassArr`'s correction is untouched -- verified safe under a combined x+y split (2,2,1) too (no zero-mass/NaN; a prior attempt that skipped the whole `syncArnBoundary` call broke exactly that by silently skipping `fltMPI(k)`, which is why this fix is scoped to the arn add-back alone). `checkFaultMPIAlignment` (`src/eqdyna3d.f90`, called right after `meshgen`) is a permanent hard-stop guard for the one case the fix does not reason about (a fault with non-degenerate y-extent on a y-boundary -- not producible by any case in this codebase today); it no longer fires for the now-correct degenerate case. Guarded by `testsys/regression/test_fault_mpi_boundary_arn.py` (decomposition-invariance: serial==xsplit==zsplit==ysplit hypocenter traction on a tiny symmetric-y case; verified to FAIL if the fix is reverted). All 8 gated compsets use asymmetric y-domains (`fltnum(3)/(4)` are always 0 for them), so the changed code path is provably never entered there. CORRECTION (v5.5.0 release audit): this fix's source (`src/meshgen.f90` dimId threading, `src/eqdyna3d.f90` checkFaultMPIAlignment) was NOT actually committed when this line was first written three commits ago at 12c8274/e6ad307 -- only the regression test and this claim were; the diff sat uncommitted in the working tree, so `testsys/run.py regression` at those commits would have FAILED on a clean rebuild. The v5.5.0 release commit is what finally lands the source fix. e2e confirmation (v5.5.0 release gate, this session, src/eqdyna to avoid the reported concurrent `bin/eqdyna` TPV29 job): `python3 testsys/run.py all` -- unit/regression/e2e all SUCCESS, 8/8 e2e cases, 26/26 check.test.py comparisons SUCCESS, including test.tpv29 matching its frozen reference. CI-BLOCKER FOUND AND RESOLVED (same audit): CI failed `test_fault_mpi_boundary_arn.py` on 12c8274/e6ad307 with ysplit shear traction 3.791069 MPa vs serial 3.791525 MPa -- diagnosed as the CORRECTION above manifesting on CI (those commits genuinely lack the source fix, so CI was correctly failing against unfixed code), NOT residual floating-point/environment noise: reproduced exactly, locally, by reverting src/meshgen.f90+src/eqdyna3d.f90 to HEAD and rebuilding clean (got the identical 3.791069/3.791525 pair); with the fix restored, serial==ysplit==3.791525 exactly, reproduced out to 8x this test's term (40 dt) with zero difference at every duration tried. Exact-equality comparison in the test is therefore correct and was left unchanged (a tolerance-loosening was drafted, then reverted, once the real cause was found). CORRECTION 2026-09-16 (item 24(a) audit, owner-verified): 'provably never entered there' / 'not producible by any case in this codebase today' HOLD for drv.a6 -- its 1 km y-domain asymmetry was, until this fix landed, the only thing keeping it out of this exact bug, not anything structural (see item 24(a)'s correction) -- but FAIL for tpv36/tpv37, which DO enter this path (dipping faults, `C_degen>3` give real nominal-grid y-extent) and are now exercised under MPI (v5.8.2 cut): `checkFaultMPIAlignment`'s guard fired there as a correct-but-unaudited refusal, downgraded to a rank-0 NOTICE and pinned by `testsys/regression/test_dipping_fault_y_split.py`. | 2, 10 | `src/meshgen.f90` MPI4arn/syncArnBoundary, `src/eqdyna3d.f90` checkFaultMPIAlignment | guarded | 2026-09-14 | `python3 testsys/regression/test_fault_mpi_boundary_arn.py` |

| 27 | RESOLVED (v5.6.0): `scripts/case.setup` now checks the generator's exit code and raises `RuntimeError` naming it, instead of continuing with a stale/partial fault-geometry file; and whatever the generator produced is then put through `lib.validateFaultRoughGeometryForCase`, so a silently-truncated file fails there too | 2 | `scripts/case.setup` | resolved 2026-09-15 | `grep -n "generateFaultInterface failed" scripts/case.setup` |

| 28 | ~~TO-DO (found during v5.5.0 release audit): TPV29's cross-code validation numbers ... are prose-only ... with no committed script to reproduce them~~ RESOLVED (`5d1a428` tooling + `d5da43e` comparison script, 2026-09-16): see the P2 row above for the full account, including a REAL correction the script's first run produced — the recorded "Mw 7.45" had no reproducible provenance (physically impossible for this run by 4.7x in moment) and is now corrected to 7.034 in both the script and item 20. This is the class of gap item 21 closed for drv.a6, landing the same way: reproducing a prose claim can falsify it, and that is the gate working, not failing | 2, 4 | `case_input/test.tpv29/README.md`, `scripts/scec/`, `testsys/parity/evidence_tpv29_scec_comparison.py` | resolved 2026-09-16 | `python3 testsys/parity/evidence_tpv29_scec_comparison.py` |

| 29 | SET ASIDE (v5.5.0 release audit, 2026-09-14): two uncommitted working-tree changes, unrelated to this release's TPV29/MPI-fix scope and not release-ready, were `git stash`'d rather than committed or discarded ("v5.5.0 audit: out-of-scope/broken WIP..." in `git stash list`): (a) `src/fortran/pydump.f90` rank-suffixed dump filenames (`.r<rank>`) — breaks `testsys/parity/make_fixtures.py`'s fixture regeneration, which still expects the unsuffixed names, and no consumer under `python/` or `testsys/` was updated to match; (b) `testsys/perf/run_elem_scaling.py`'s `--max-others`/`CORE_BASE` addition — `CORE_BASE` is dead (referenced only in comments), the mpirun command has no `--cpu-set`, and the script's own comment elsewhere says Open MPI ignores such pinning attempts anyway, so the claim that sibling jobs "do not contend for cores" does not hold (default `--max-others=0` means nothing measured so far is tainted, but the rationale for `>0` is false). Needs its own follow-up session, not folded into a release. RESOLVED 2026-09-16: see the P2 row above -- both target files are now deleted from the tree, so this stash (and 3 older ones found alongside it) are unapplicable by construction. **CLOSED 2026-09-17: owner approved the drop and it has been executed** -- verified fresh this session, `git stash list` on current HEAD returns EMPTY (all four stashes gone) | 1, 2 | `src/fortran/pydump.f90`, `testsys/perf/run_elem_scaling.py` | resolved 2026-09-17 | `git stash list` |

| 30 | ~~BUG (instrumentation): `compTimeInSeconds(2)` is corrupted~~ FIXED (2026-09-16, 8839638) -- and it was **511x**, not the '<=1% bias, harmless' this row claimed. ONE global `startTimeStamp` (`globalvar.f90:201`) was written by EIGHT sites across six files, each pairing it with a different counter, so any nesting silently truncated the OUTER measurement. Real nesting: `eqdyna3d.f90:68-70` wraps `assembleGlobalMass`, which calls `MPI4NodalQuant` twice (`:41,:42`), each resetting the global -- so slot 2 timed only from the LAST such call. Measured before/after on test.tpv8 at 4 ranks: **compTimeInSeconds(2) 0.0001 s -> 0.0511 s**. Each site now uses a local `tStageStart`; `startTimeStamp` survives only in eqdyna3d.f90 where no nested writer remains. Output-neutral, verified: test.tpv8 x fortran max|diff| 3.051760e-11, identical to the pre-change sweep. **NEW, and the reason this went undetected:** `writeCompTime` (`globalvar.f90:109`) is initialised to 0 and read from NOWHERE -- not bGlobal.txt, not user_defined_params.py, not the environment. The per-stage timing output is unreachable without editing source and rebuilding. Giving it an input slot is a case-input contract change and is NOT done; until then every number in `compTimeInSeconds` is invisible by default, which is how a 511x error survived. | 2, 4 | `src/fortran/globalvar.f90:109,201`, `src/fortran/eqdyna3d.f90`, `driver.f90`, `assembleGlobalKU.f90`, `assembleGlobalMass.f90`, `calcHourglassResist.f90`, `faulting.f90` | fixed 2026-09-16 | `grep -c tStageStart src/fortran/*.f90` |

| 31 | RESOLVED (v5.6.0): `case_input/test.tpv29` now ships the official surface at BOTH 100 m and **50 m** (`bFault_Rough_Geometry.tpv29.50m.txt`, 14 MB, exact 2:1 decimation of the official 25 m file), so the FULL_SPECS 50 m tier runs from a clean checkout with no 66 MB download. The 50 m file was re-derived independently from `scratch/tpv29/downloads/tpv29_tpv30_geometry_25m_data.txt` via `convertOfficial25m` and is byte-identical to the one the spec run used. `tpv29GeometryTools.shippedSourceForDx` picks the coarsest shipped source that reaches the requested dx; decimations from either source are identical (both land on the same official 25 m nodes) -- the dx=500 `bFault_Rough_Geometry.txt` md5 is 00aea9b7b2bdb333fb58a42606ee5290 before and after, so the gate is unmoved. `convertOfficial25m` is now a documented first-class entry point with a CLI, and validates what it writes. The general form is guarded: compsets declare `par.faultGeometrySourceDx` / `faultGeometrySourceAvailableDx` and `lib.requireFaultGeometryResolution` refuses any `par.dx` finer than, or not a multiple of, the source spacing, naming the requested dx, the native spacing, the spacings actually available and what would satisfy it | 2, 3 | `case_input/test.tpv29/`, `scripts/lib.py`, `scripts/defaultParameters.py` | resolved 2026-09-15 | `python3 -c "import sys;sys.path.insert(0,'scripts');sys.path.insert(0,'case_input/test.tpv29');import tpv29GeometryTools as g;print(g.faultGridForCase(50.0)[0].shape)"` |

| 32 | RESOLVED for the GATE (2026-09-16), OPEN as a numerics question. drv.a6 is gated again and passes with **nothing moved**: python-numpy 423 flips and python-jax 415 against the untouched 450 budget, fortran 0 flips (bit-exact at the reference's own 4-rank decomposition). The 461 that had failed was the PORT deviating from Fortran -- `port_rsf.py`/`port_tp.py` ended each step with `force * inv_mass` while `driver.f90:30` DIVIDES; the unified driver divides for every friclaw, so friclaw 1 is untouched and friclaw 4/5 moved toward the reference. Briefly withdrawn as `matrix.REFERENCE_ONLY` on 2026-09-15 and restored one commit later (87a32e9 -> 1f808f8); that machinery is removed, since an empty withdrawal table makes its own tests vacuous. STILL OPEN, and not claimed closed: Fortran compared against its own 4-rank reference from a SERIAL run still gives **329 arrival flips of 5151**, max 2.12e7, p50 4.03e-03, median arrival shift 0.0417 s = one time step. The decisive experiment is NOT the dx=250 refinement first queued here -- it is a **serial Fortran reference**, which removes the decomposition term entirely and isolates the port's own contribution (the python columns' ~415-423 decomposes as ~329 inherited from serial-vs-4-rank plus ~90 actual port difference). Check item 24(a) FIRST: it claims on-fault tractions resolve to HALF for `C_elastic==0`, and drv.a6 IS `C_elastic==0` -- if live, the case is simply running wrong and neither bistability nor under-resolution is the explanation. | 2, 4, 5 | `testsys/matrix.py` DRV_A6, `testsys/compare.py` flip_budget_gate, `testsys/parity/evidence_drv_a6_chaos.py` | until the numerics question is settled | 2026-09-16 | `python3 testsys/parity/evidence_drv_a6_chaos.py` |

| 33 | TO-DO (raised by the owner 2026-09-16): **re-measure JAX-CPU core scaling properly. Every scaling number this repo quotes is stale, unverified, or taken under load -- do not cite them until this lands.** WHAT IS ACTUALLY ESTABLISHED: one ratio, measured 2026-09-16 on the current code -- `test.tpv8`, **193 ms/step pinned to 1 core vs 13.8 ms/step unpinned = 14x**. That is all. WHAT IS NOT: the `2.00x / 3.96x / 7.48x on 2/4/8 cores` curve and the `~2.5x plateau when memory-bound` both PREDATE the Python restructure and the HLO-literal fix and have not been reproduced since. They were quoted in this session as if current; they are not. WHY THE OLD CURVE IS SUSPECT MECHANICALLY: this box is 8 NUMA nodes of 8 cores each (2 sockets x 32, AMD EPYC 7532, 1 thread/core, ~128 GB per node). The inherited knee sits at exactly 8 cores -- the NUMA node size -- so it may be measuring remote-memory cost rather than any property of the solver. One process realistically ceilings at one socket (32), and 64 was never a meaningful denominator for efficiency. THE EXPERIMENT: (1) `taskset` 1/2/4/8 cores WITHIN one NUMA node (cpus 0-7); (2) 8 cores deliberately SPREAD one-per-node (0,8,16,24,32,40,48,56) -- same core count, maximum NUMA distance, so core count and locality are separated; (3) 16 and 32 within one socket. Use a case with enough work per step -- **tpv104 or drv.a6, not tpv8** (1891 fault nodes runs out of parallel work early, which is a plausible alternative explanation for the knee). Report per-step cost by DIFFERENCE over two step counts, as `testsys/perf/run_perf.py` already does, so XLA compile is subtracted. Repeat each point and record `load_avg` per run. **REQUIRES AN IDLE BOX.** Every figure above was taken with 10 other users on the machine at load 6-11, plus this session's own gate. An idle box is a precondition, not a nicety -- a memory-bandwidth measurement under someone else's memory traffic measures them. **UPDATE 2026-09-17: two real tooling defects found+fixed in `testsys/perf/run_numa_scaling.py` (commit `03ba055`), not the measurement itself.** F1: the config generator produced an all-64-core configuration, contradicting this item's own "at most 32, not 64" constraint above -- removed. F2: configs jumped from spread-8 straight to socket-32 with no 16-core point, a 4x gap across exactly the region this item asks about ("16 AND 32 within one socket") -- added a generic two-NUMA-node ("multi-node-16" on this box's topology) point between them. Verified by `ast.parse` + `--help` only -- no idle box available this session to run the actual measurement; load-ceiling/override logic byte-for-byte untouched. The tool itself was wrong before today (would have misled even on an idle box) and is now fixed; the measurement is still outstanding, still blocked on load (ceiling unchanged at 1.0, load ~2.4-3.2 all session from other tenants). **UPDATE 2026-09-17 (commit `d70811d`): MEASURED, ROOT-CAUSED, PARTIALLY FIXED.** Owner's goal reframed mid-session to "make jax and numpy scale well" (the measurement above was always instrumental to that, not the deliverable); landed via mira-volkov, verified by wei-lin before landing. `testsys/perf/run_scaling.py` rewritten: `PY_THREADS` extended `[1,2,4,8]` -> `[1,2,4,8,16,32]` to match `FORTRAN_RANKS` (the tool previously could not say whether either python backend reaches 32 cores at all); bare `taskset` replaced with `numactl --cpunodebind/--membind` (binds CPU+memory together) on both the python path and the Fortran/MPI path (a new numactl-rankfile mechanism per rank, since a bare `mpirun --bind-to` can be reissued past an outer numactl restriction by OpenMPI's own hwloc binder); compact vs spread placement policies added so a knee can be attributed to locality vs out-of-parallel-work; reuses (does not duplicate) `run_numa_scaling.py`'s own busy-check. Measured (disclosed caveat: `--busy-ceiling 0.9` override, not the tool's strict 0.2 default, since ~9 other tenants held the box all session -- cross-validated two independent ways, exact ms/step numbers should be retaken on a quiet box as a follow-up, not treated as final): jax gains a real **2.35x at 32 cores** (cross-checked against this session's earlier clean 1-4-core numbers); numpy **anti-scales**, never faster than 1 core, mechanism verified by direct microbenchmark -- OpenBLAS reads its thread count at `import numpy` time only (confirmed by testing env-var-after-import vs numactl-pinned-before-start, not asserted). JAX's own HLO graph was dumped and read directly: one `fori_loop`, 1717 fusion + 556 scatter ops on arrays up to `f64[3818584]` per step -- real, large-scale structural parallelism exists, ruling out "nothing to parallelize" as the explanation for the plateau past 8 cores (memory bandwidth and/or the ~556 per-step scatter-accumulations are the more likely binding constraint, not chased further this pass). Both known project failure modes (a jitted function closing over an array paying an HLO-literal cost; `jax.jit` built inside a run function recompiling every call) were checked directly against `backend.py`/`eqdyna3d.py` and RULED OUT, not assumed clean. **Fix landed:** `_narrow_numpy_affinity()` in `src/python/eqdyna/eqdyna3d.py` (numpy backend only) pins the process to a single cpu, bounding the NUMA-migration worst case (a single-threaded process with wide affinity can be OS-migrated between NUMA nodes mid-run, stranding its memory -- measured ~2x regression from this specifically). This does NOT make numpy scale -- its hot kernels (`calcHourglassResist`/`assembleGlobalKU` elementwise ops, `np.add.at` scatter-adds, 85%+ of a step per item 40's profile) are single-threaded regardless of core count; a real fix needs a kernel rewrite, explicitly out of scope here. **A real regression caught by wei-lin's own review before it landed, not by the subagent's own testing:** the first version of the affinity fix picked `min(current)` -- the same lowest-numbered cpu for every process, verified directly (`/proc/<pid>/status` on two concurrently-launched processes both showed `Cpus_allowed_list: 0`) to collide every concurrent numpy cell in `testsys/e2e/run_e2e.py --jobs N>1` (this project's own wider local gate, `run.py all`) onto one physical core while dozens sit idle -- this exactly explained a ~10%-cpu-each pileup observed earlier in the session. Fixed to `ordered[os.getpid() % len(ordered)]` (still narrows to exactly one cpu, still fixes the NUMA-migration case, but different processes now land on different cpus -- re-verified: two concurrent processes now on cpus 25/27, not both 0). **Verification, independent of the subagent's report:** her own mission disclosed only 1 of 10 numpy cells (`tpv8`) re-checked against its frozen reference before she had to kill her own `run.py all` under this session's box contention; wei-lin ran the remaining 9 fresh on the corrected affinity code (`drv.a6, tpv10, tpv104, tpv1053d, meng2023a, meng2023cb, tpv29, tpv36, tpv37`, all python-numpy): 9/9 SUCCESS against their existing bounds, unmoved -- no reference moved, no bound loosened. jax/fortran cells are provably unreachable by this change (gated on `backend=='numpy'` in the diff itself), correctly not re-run. Diffed every changed file against current master before landing: zero staleness. Fresh `python3 testsys/run.py unit regression`: SUCCESS both tiers. **Still open, natural next step, not urgent:** re-measure the actual scaling curve on a genuinely idle box (this session never had one) for trustworthy ms/step numbers rather than the `--busy-ceiling 0.9`-override figures; jax's plateau past 8 cores is diagnosed as structural (real ops exist per the HLO dump) but not further isolated to memory-bandwidth vs scatter-accumulation specifically -- a controlled follow-up experiment, not queued. | 4, 6 | `testsys/perf/run_perf.py`, `testsys/perf/run_numa_scaling.py`, `testsys/perf/run_scaling.py`, `testsys/perf/measured_runs.md`, `src/python/eqdyna/eqdyna3d.py` | until re-measured on an idle box | 2026-09-17 | `python3 testsys/perf/run_scaling.py` (see file for placement-policy flags); `python3 testsys/run.py unit regression` |

| 35 | OPEN (2026-09-16, found auditing the v5.8.2/v5.8.3 CI run history): three commits each have TWO push-triggered CI runs created within 1 SECOND of each other, both FAILURE -- `335e21d` (2026-09-15T10:14:09/10Z, runs 34956858515/34956859450), `238f1ac` (2026-09-15T02:40:59Z both, runs 34922110783/34922111214), `9950ae1` (2026-09-15T02:19:10/11Z, runs 34920713857/34920714277). Verified via `gh run list`, not just asserted. Distinct from item 32's/this release's `bccfb84` two-runs-12-minutes-apart pattern (that one has an identified mechanism -- see rule 15's amendment -- this one does not: same-second duplicate triggers on a single push look like a client- or webhook-side double-fire, not a tag-vs-branch distinction). Cost is wasted runner-minutes and a harder-to-read `gh run list`, not incorrectness -- no release was affected. Not investigated further; cheap to reproduce next time it recurs | 6 | `.github/workflows/test.yml` | not investigated | 2026-09-16 | `gh run list --repo EQdyna/EQdyna --json databaseId,headSha,createdAt \| jq` and look for duplicate headSha within 1-2s |
| 39 | RESOLVED 2026-09-17: `test.meng2023a`/`test.meng2023cb`/`test.tpv29`'s `CASE_BOUND=THRESHOLD` was justified by "no python run has ever been measured for these" -- dead since haruto's CI_CELLS widening pass (2026-09-16) measured all three and confirmed each passes its cell. Tightened the same way item 19(a) tightened tpv36's: worst observation x tpv8's own headroom ratio (~121.5x), rounded to the nearest bound already in use at that magnitude. `test.meng2023a`/`test.meng2023cb` -> `1e-6` (worst observed 4.63e-09/5.05e-09, same bound as tpv10/tpv36); `test.tpv29` -> `1e-10` (worst observed 1.28e-13, rounded to more headroom than the formula's raw 1.6e-11 since these are near-machine-epsilon diffs). Verified fresh, not reused: see this row's own edit history / the session log for the actual numbers this session measured before landing | 5, 6 | `testsys/matrix.py` CASE_BOUND | resolved 2026-09-17 | `python3 testsys/e2e/run_e2e.py --cases test.meng2023a,test.meng2023cb,test.tpv29 --backends python-numpy,python-jax` |
| 40 | MEASURED 2026-09-17 (relayed via haruto-nakamura's report this session, NOT independently re-run/re-verified by this session -- treat as a hypothesis carried from his report until independently reproduced): the ~5-6x tpv29 numpy-vs-jax gap is diagnosed, not fixed. Numbers as reported: numpy pinned 2072 ms/step (linear over 3 points, repeatable to 0.2%); jax pinned 524 ms/step (4.0x vs numpy pinned); jax unpinned 365 ms/step (5.7x vs numpy pinned -- this is the "~6x" this row previously recorded). Fixed overhead is NOT the explanation: numpy's fixed cost is ~1% of its run, jax's is 12-15% -- subtracting it makes numpy look WORSE, not better, so the earlier compile-amortization hypothesis for the gap does not hold as the main driver. **KEY FINDING:** unpinned numpy is UNSTABLE, not a fixed quantity -- 2.08 to 3.75 s/step measured with NO dependence on step count, while pinned numpy is linear to 0.2%. This item's own previously-recorded "950 to 1711 s" wall-clock spread for tpv29 IS this same instability (1711/950 = 1.80, matching the worst-unpinned/pinned ratio). So CI's ~1016s tpv29 critical path is a PLACEMENT LOTTERY (which cores the scheduler happens to put the process on), not a fixed cost of the port -- this changes what that CI number means and is stated plainly here, not buried. Optimisation targets identified (measured, NOT yet applied -- do not read this item as closed, only diagnosed): `assembleGlobalKU.py:466` `calcHourglassResist` 38% (1.094 s/step); `assembleGlobalKU.py:256` `assembleGlobalKU` 29% (0.828 s/step), underneath `c_einsum` 15% and `numpy.ufunc.at` 10% (31 calls/step); separately, `meshgen.py:323` `build_node_coordinates` is an 8.76s ONE-TIME cost (not per-step, won't show in a per-step metric -- flagged explicitly so nobody "optimizes" it and measures zero per-step change), of which `_fortran_nint` is 3.72s (1,063,959 calls) and `insert_fault_interface` is 5.94s (908,211 calls), both pure-Python per-node loops, IDENTICAL in the jax profile (3.70/5.90s) -- i.e. backend-independent, so fixing it helps BOTH backends and is the cheapest real win identified. No optimization applied yet: queue order is tooling + board rows first (done this session), then any port change needs the full three-backend gate before it can be trusted, and the box cannot give a trustworthy before/after measurement right now anyway (same load-contention issue as item 33) | 6 | `src/python/eqdyna/assembleGlobalKU.py`, `src/python/eqdyna/meshgen.py`, `testsys/perf/` | measured, not applied | `python3 testsys/e2e/run_e2e.py --cases test.tpv29 --backends python-numpy,python-jax` (compare wall-clock; relayed numbers not independently re-run) |
| 38 | OPEN (2026-09-16, found on v5.8.7's tag-triggered CI): `test_stop_exit_status.py`'s real-binary probe flaked ONCE under haruto's new `unit-regression` job (downloads `bin/eqdyna` via `download-artifact` from `build`, doesn't build it itself): exit code was correct (21, `ERR_INPUT_FILE_MISSING`, matching the registry) but `'EQdyna: FATAL' not in out` fired -- the FATAL text wasn't in the captured stdout+stderr that run. Immediate `gh run rerun --failed` on the identical commit passed clean. Not investigated further (one occurrence, self-resolved, not blocking); plausible mechanism is an `mpirun -np 2` multi-rank stdout interleaving/flush race, unrelated to the artifact-download architecture but possibly more likely to surface under it. If it recurs, check whether `capture_output=True` reliably captures both ranks' output before the process is reaped | 2, 6 | `testsys/regression/test_stop_exit_status.py:283` (`probe_real_binary`) | one occurrence, not reproduced | `gh run view <failing-run-id> --log-failed \| grep FATAL` |
| 34 | OPEN (2026-09-16): **grid-scale stress ringing at 500 m is stabilised but not damped.** After the rupture front passes, on-fault shear rings at ~1.4-3.2 Hz with 0.7-5.6 MPa peak-to-peak. `Vs/(2*dx) = 3.46 Hz` at dx=500 m, so this is the mesh's shortest resolvable wavelength. MEASURED, post-arrival window [t_arr+0.15, t_arr+1.6] s, linear detrend only: tpv36 (wedge) 0.68 MPa / 3.17 Hz / 1.67 pct of peak tau; tpv37 (wedge) 0.76 / 2.58 / 1.77; **tpv10 (hex, dipping) 5.60 / 2.37 / 9.49**; **tpv104 (hex, vertical) 4.79 / 1.41 / 5.86**; tpv8 (hex, vertical) **0.004 / 1.39 / 0.01** -- essentially ring-free. WHY IT IS NOT SUPPRESSED, and this part IS settled: the default hourglass control is `C_hg=1`, KF78, STIFFNESS-based and non-dissipative by construction. Its only damping is the Rayleigh term inside the hourglass force, `dl = disp + rdampk*vel` with `rdampk = 0.1*dt` (`readInputFiles.f90:133`), giving zeta = 0.5*(0.1*dt)*omega_grid = **0.0065, i.e. 0.65 pct of critical**, an e-folding after ~24 cycles. The mode cannot grow but barely decays. AND ZETA IS RESOLUTION-INDEPENDENT: `rdampk ~ dt` while `omega_grid ~ 1/dx ~ 1/dt`, so they cancel -- 0.65 pct at both 500 m and 50 m. Refining does NOT damp this mode; it moves it to a band carrying less energy, which is why the published 50 m run looks flat rather than being better damped. THE LEAD: `C_hg=2` is a genuine VISCOUS hourglass control (`kapa_hg=0.1`, Goudreau and Hallquist 1982) that IS dissipative, and **no case in this repo sets it** -- that path has never been exercised. One run of tpv37 with `C_hg=2` would say whether it damps this at the default kapa_hg. WHAT IS NOT KNOWN: why the amplitude varies 1000x across cases (tpv8 0.01 vs tpv10 9.49). Element type is NOT the discriminator -- the degenerate-wedge cases ring LESS than two of three hex cases, and a degenerate wedge is still an 8-node element of the same shape family, so the hex hourglass modes apply normally. friclaw, dip, insertion-vs-degeneration and nucleation sharpness are all confounded at n=3-9 stations per case. Three mechanisms were proposed and measured WRONG before this (front element-crossing, wedge hourglass mismatch twice); do not add a fourth without a controlled experiment. CONSEQUENCE FOR THE GATE: the 500 m reference files CONTAIN this ringing, so the sweep compares ringing against ringing and is structurally blind to it. Acceptable for a regression check; not an accuracy claim. | 2, 4 | `src/fortran/calcHourglassResist.f90`, `src/fortran/globalvar.f90` (C_hg, kapa_hg, rdampk), `src/fortran/readInputFiles.f90:133` | until characterised | 2026-09-16 | one run of tpv37 with par.C_hg=2 vs the default |

| 36 | OPEN, NOT DECIDED (2026-09-16) -- given its own numbered row so it is findable by grep rather than buried only in the v5.8.2 Tasks-done paragraph (~line 172) and `docs/SESSION_LOG_2026-09-16_v5.8.2-followon.md`: whether to rewrite git history to purge the ~1.16 GB of `scec_archive/` blobs that `f2c9851` accidentally tracked (the `.gitignore` inline-comment bug, fixed in v5.8.2; the tracked files were `git rm --cached`'d, but the blobs stay reachable in history through the tags below). FRESH NUMBERS (verified 2026-09-16, this session, commands in the Command column): repo is public, 2 forks, 24 stargazers, `diskUsage` 290272 KB (~290 MB); `git count-objects -vH` reports size-pack 283.57 MiB across 4392 objects in one pack; traffic over the last 14 days is 976 clones, 158 unique cloners. TRADEOFF: a history rewrite (`git filter-repo`/BFG) would diverge the history of all 158 recent unique cloners and both forks -- neither is fixable from this side -- and would move the SHAs of the `v5.8.0`/`v5.8.1`/`v5.8.2` tags (the 3 of this repo's 20 tags cut after `f2c9851`) and their GitHub Releases. It would also NOT shrink GitHub's stored copy by itself: unreachable objects from a rewrite stay in GitHub's existing pack file until GitHub Support runs gc server-side, so the disruption is paid up front and the bytes are not reclaimed by the rewrite alone. Against that: 290 MB is roughly 29% of the 1 GB size GitHub starts commenting about -- not yet an urgent size problem on its own. **Left explicitly OPEN -- the owner has not ruled on it.** This row records the numbers and the tradeoff, not a recommendation for or against, either way. SEPARATE, NOT TO BE CONFLATED: item 29's 4 confirmed-unapplicable `git stash` entries have a recommended `git stash drop` that is likewise not yet decided (full evidence already in item 29's row, not repeated here) -- all four stay in place until the owner rules. | 4, 12 | `.git/` (repo history), `docs/SESSION_LOG_2026-09-16_v5.8.2-followon.md` | until decided | 2026-09-16 | `gh repo view EQdyna/EQdyna --json isPrivate,forkCount,stargazerCount,diskUsage; gh api repos/EQdyna/EQdyna/traffic/clones; git count-objects -vH` |

**Deferred**: none.

## Historical SCEC submissions

`scec_archive/` (gitignored, at repo root) holds EQdyna's own SCEC/USGS code-
verification submissions in raw upload format, one directory per
benchmark/version/resolution — e.g. `scec_archive/tpv29/eqdyna-v3.1-100m-2015/`
(the published `dliu` entry; its cplot verified byte-identical to the copy the
cvws public area serves). Treated as read-only historical reference for
comparing new runs against past published standing. Not tracked by git and not
reproducible from the repo — back it up independently.

## Release roadmap

Work is grouped into release campaigns; each ships only when its own gate is
green (fast tiers + e2e + CI) and its claims are test-backed.

| Release | Theme | Contents |
|---|---|---|
| v5.5.0 (cutting now) | TPV29 revival + MPI fix | TPV29 gated (dx=500, reference frozen, FULL_SPECS at 50 m); fault-on-MPI-boundary arn bug fixed + hard-stop guard + regression test; insertFaultType=3 documented; memory/cell report corrected; compset figures |
| v5.6.0 | Fault-geometry robustness | Validator for bFault_Rough_Geometry.txt (header vs case grid, dx/fxmin/fzmin, row count, finite values, derivatives vs finite differences with a self-consistent curvature bound, per-cell offset limit, provenance-sidecar cross-check), called from case.setup for EVERY insertFaultType>0; Fortran-side checks in read_fault_rough_geometry (was reading nnx*nnz rows unverified) with a deterministic non-zero stop; `scripts/convertFaultGeometry` for arbitrary supplied surfaces; provenance sidecars on every generated file; case.setup made idempotent and non-clobbering via `par.faultGeometryWriter` (compsets no longer write geometry as an import side effect -- that is what silently replaced a deliberately placed 50 m TPV29 surface); unit tests per failure mode plus a Fortran regression guard; items 27 and 31 |
| v5.7.0 | Viscoplastic path + TPV30 | item 24 (Tv input slot, setPlasticStress depth taper, the +7.3215 constant, lib.py rho, plastic-output window), item 23 (Drucker-Prager kernel unit test), then TPV30 promoted and gated (draft built, reproduces the elastic-vs-plastic contrast) |
| v5.8.0 | Cycle + archaeology | item 16b (mode-2 restart gate coverage - the EQsimu hand-off path has none today), item 19 (TPV2802_50_a6 gating; TPV2800/2801 dead-branch decision) |
| v5.9.0 | Benchmark suite build-out (owner-queued, after the viscoplastic path) | Compsets + gated references for the remaining benchmarks EQdyna has submissions for: **TPV30** (rough + viscoplastic, draft already built and validated against TPV29's contrast), **TPV34** (Imperial Fault in the real CVM-H 3D velocity structure - needs the 3D-velocity input path exercised), **TPV35** (Parkfield 2004 M6 - SCEC's first VALIDATION benchmark, compares against recorded ground motion, not just cross-code), **TPV36/TPV37** (15-deg shallow-dipping thrust; submissions are modern-code v5.3.3 at 50 m, so their published standing is a current-code datapoint and the best available cross-check). Each: fast-tier compset at a gate-sized dx with frozen reference, FULL_SPECS entry at the spec resolution, compset README with the published standing from scec_archive/, and scoring against the public cvws metrics where a metric page exists |
| v5.10.0 | Fully dynamic cycle support (EQsimu) | Gate the mode=2 restart path using `liu2020.fdc.planar` (single fault, planar, dx=300, RSF aging, term 150 s - the Liu et al. 2020 EQsimu configuration) as the canonical cycle compset. Two layers: (a) EQdyna-only gate from a FROZEN EQquasi hand-off committed as test data (fault.dyna.r.nc + restart netCDF) - fast, exercises netcdf_read_on_fault_eqdyna_restart and the mode=2 path that has zero coverage today; (b) a periodic coupled verification running the real loop across EQdyna / ~/seas_bp10_eqquasi / ~/eqsimu, not in CI. Also covers liu2020.fdc.rough.250 and bp1001.fdc.rough.250 once (a) works.
**Plus build `liu2020.fdc.kink.300`** — the missing EQdyna half of the Liu et al. 2020 paper's kink-bend fault: EQquasi already has `liu2020.qdc.kink.300` and `.600` (an active revival there), so the pair completes the coupled configuration. It is also the only route to gated coverage of `C_degen` wedge degeneration, which no current gated case exercises despite v5.3.3 re-incorporating it for TPV36/37. Interface contract (frt.txt format, restart nc variables, fric slot layout) written down as part of this release |
| v6.0.0 | Multi-fault, then multi-fault cycles | Two stages. (1) Multi-fault dynamic rupture: fix items 17 and 7/9/10/11, and adopt EQquasi's ALREADY-DEFINED fault-major layout `on_fault_vars(ntotft, nfz, nfx, 100)` in EQdyna's netcdf writer and `netcdf_read_on_fault_eqdyna` (EQdyna is `(nfz, nfx, 100)`, single-fault; the hardcoded fault index 1 of item 7 lives in exactly that reader) rather than inventing a second convention - reference compsets `bp1002.qdc.2500`, `test.stepover.qdc.{con.}1000` in ~/seas_bp10_eqquasi/compset. (2) Multi-fault FULLY DYNAMIC cycles: build the EQdyna-side `bp1002.fdc.*` counterpart (EQquasi has only qdc step-over today) and extend the v5.10.0 mode=2 hand-off to carry multiple faults. Cross-repo campaign (EQdyna / ~/seas_bp10_eqquasi / ~/eqsimu) with a written interface contract and matched tags - major bump, coordinated release |

Not on the roadmap (external, idle-window, or owner-scheduled): strong-scaling
measurement (`testsys/run.py scaling`, needs an idle machine; a cells-per-rank
study is in flight), the TPV29 50 m spec run (48 ranks, ~5 h, running) and its
scoring against the public cross-code matrix, spec-resolution verification runs
on HPC, TPV38 (awaiting SCEC's TSurf release).

## Goals

- **Python EQdyna (vectorized)** — long-term goal set 2026-09-12: a vectorized
  Python implementation of EQdyna, developed under `python/` at repo root
  (ALL Python solver development lives there). Phase 1: feasibility spike —
  port the hot kernel + time loop for `test.tpv8`, parity-gated against the
  Fortran `frt.txt` golden gate, with wall-clock comparison (NumPy first,
  JAX/GPU assessment after). Fortran `src/` remains the production/HPC engine;
  the Python solver targets test-scale cross-verification first, GPU later.

## Tasks done

| Date | Task | Ref |
|---|---|---|
| 2026-09-17 | v5.11.0 cut (minor, owner-requested by name, 31 commits since v5.10.0): CI coverage widened 10 -> 25 of 30 cells (`test.tpv36`/`test.tpv37` python-numpy/python-jax admitted on measured 2.91-4.34 GB peak RSS, commit `af892df`). Item 33 landed: `run_scaling.py` rewritten (`PY_THREADS` to `[1,2,4,8,16,32]`, `numactl --cpunodebind/--membind` replacing bare `taskset`, compact/spread placement) -- jax measured 2.35x at 32 cores; numpy's anti-scaling root-caused to OpenBLAS reading its thread count only at `import numpy` time, bounded (not fixed) by `_narrow_numpy_affinity()` pinning the numpy backend to one cpu (commits `03ba055`, `d70811d`, `0b4684c`) -- measured under a `--busy-ceiling 0.9` override on a box carrying ~9 other tenants all session; re-measurement on a quiet box still outstanding. Item 40 (RELAYED, not independently re-run this session -- treat as a working hypothesis): tpv29 numpy-vs-jax gap pinned at 4.0x, not the ~6x previously on the board; CI's python-numpy cells now thread-pinned (`OMP_NUM_THREADS`/`OPENBLAS_NUM_THREADS`/`MKL_NUM_THREADS`/`NUMEXPR_NUM_THREADS=1`, commit `0b4684c`, `.github/workflows/test.yml`) to remove a placement-lottery effect. Item 29 closed (four confirmed-unapplicable stashes dropped, `git stash list` verified empty); item 19(c) closed (TPV34/TPV35, owner decision -- sources predate this repo, not recoverable). First net-negative pruning commit landed (`9fe4a54`, Dockerfile/test.yml/publish.yml comment-bloat cut, -62 net lines, no logic change). `test.tpv30` investigation CLOSED AS OPEN 2026-09-17 (owner decision, final for now): a fault-local equilibrium defect in the `C_elastic=0` path is resolution-independent per the owner's own published TPV30 data at three resolutions (100m/50m/25m) and root cause is not located; both proposed fixes (a discrete-equilibrium correction-offset and a literal Method-1 architectural switch) were built, verified, and REJECTED, neither lands; `test.drv.a6`'s gate is internally self-consistent (numpy/jax match its own Fortran reference) but not externally validated against this behavior, and its reference/bound stay unchanged while this is open; decision is to leave the investigation open and move on, not to keep re-chasing it -- full consolidated account in item 39's own row. tpv30 remains UNREGISTERED. Fresh gate this release: unit+regression all SUCCESS, e2e 30/30 cells SUCCESS, wall clock 2263.3s, on master `bd9d81e`, CI confirmed green on `72a353b` (run `35254856795`). | v5.11.0 |
| 2026-09-17 | v5.10.0 cut (minor, owner-requested by name): item 19(a) closed for real on BOTH cases now -- `test.tpv37` registered onto the gate (pure registration, no port work: both `src/fortran/faulting.f90:405` and `src/python/eqdyna/faulting.py:109` already carried the `TPV==37` branch from TPV36's own landing), bringing the sweep to 10 cases x 3 backends = 30 cells. Item 24(b)(c)(e)(f) closed: Tv, the off-fault deviatoric depth taper, the case's own shear modulus, and the plastic-strain output window are now real case inputs, each defaulting to the constant the solver used to compile in; `bGlobal.txt` gained a new trailing block that `case.setup` writes unconditionally and the solver reads unconditionally -- an INPUT-FILE CONTRACT CHANGE, refused loudly on a stale file (new `ERR_INPUT_FILE_STALE=22`), never silently defaulted. Runtime banner (`src/fortran/eqdyna3d.f90:17`) had drifted to "5.9.0" while `VERSION` said 5.10.0 -- caught by `test_version_banner.py` on this release's own gate run (the exact drift class the guard exists for, cf. v5.6.0's 2-release-stale banner), fixed in the same commit per rule 11. Fresh gate this release: unit 244 passed, regression all SUCCESS (including `test_version_banner.py` after the fix), e2e **30/30 cells SUCCESS** in 2123.2s wall clock -- `test.tpv37` fortran bit-exact (0.0), numpy 6.929140e-09, jax 5.256410e-09, all against bound 1e-6; `test.tpv36` unmoved at 7.894064e-09/5.867293e-09 | v5.10.0 |
| 2026-09-17 | CI widened 10 -> 19 of 24 cells (haruto, per owner instruction, on `haruto/ci-matrix-optimization`, commit 6bb2485). The per-job-memory reasoning that had excluded tpv10/tpv104/tpv1053d x python-jax was stale once the CI matrix (previous entry) gave every job its own 7 GB and confirmed cells run one at a time within a job regardless (job logs: wall clock == sum of per-cell seconds, no concurrent-cells line) -- a job's peak memory is its single largest cell, not a sum. Newly measured and added to `matrix.MEASURED_PEAK_RSS_GB` (`/usr/bin/time -v`, full-length, one at a time, this box, against the CURRENT tree -- a first attempt ran against the pre-revert tree with mira's since-reverted C_degen merge still on it and was discarded, rule 4): meng2023a numpy/jax 2.545/4.025 GB (313.4/70.7 s), meng2023cb numpy/jax 2.543/4.020 GB (419.7/65.1 s), tpv29 numpy/jax 2.705/4.052 GB (1711.0/198.0 s) -- all independently PASSED their bound via `testsys.compare.compare_cell` on the same run. `test.tpv36` was measured too in an earlier draft of this pass, then DROPPED entirely (not merely unused) once mira's C_degen merge that gated it was reverted mid-session for an unrelated real regression (`test.tpv8 x python-jax` 4e-10 -> 1e-7, no wedge elements involved) -- rebased onto the revert (`1654998`) before finishing. Still excluded: `test.drv.a6` x python-jax (9.57 GB, the one genuine memory exclusion, not chased per owner instruction) and x python-numpy (never measured, not chased); `test.tpv10`/`test.tpv104`/`test.tpv1053d` x python-numpy (never measured -- the rule stays literal, assuming numpy tracks jax's number is the guess rule 6 forbids). Workflow: `e2e-ci-python` split into three jobs by measured cost (`e2e-ci-python-cheap`, `-meng`, `-tpv29`) since tpv29 x numpy alone (950.3 s on CI) is an order of magnitude past everything else and cannot be shrunk by splitting -- isolated so its cost is the critical path on its own. `test_ci_workflow_coverage.py` needed a real fix first: its line-based parser was matching this change's own prose comments (which describe `run_e2e.py --ci ...` invocations) as if they were invocations, an 8-way false overlap; fixed by stripping `#`-onward before matching, same as `test_sweep_core_budget.py`'s existing comment-stripping. MEASURED before/after, pushed branch not this box: 660s (1-job baseline) -> 303s (10-cell matrix, run 35151395127) -> 1020s (19-cell widened, run 35175181280, all 7 jobs green). The increase from 303s to 1020s is coverage growth (9 more cells, mostly the previously-orphaned jax cells plus meng2023a/cb and tpv29's newly-measured columns), not a regression -- tpv29 x python-numpy's isolated job (950.3s solve, matches the ~1.8x local-to-CI ratio already seen elsewhere) is the entire new critical path and cannot be reduced further without either splitting tpv29 itself (not possible, one sequential run) or reducing what CI covers, which was explicitly ruled out. Value already demonstrated in evidence, not just principle: the C_degen regression above (10x over bound) was caught by a full LOCAL sweep specifically because `test.tpv8 x python-jax` -- one of the few python cells CI ran before this change -- would otherwise have been invisible to CI until someone ran the 20+ minute local sweep by hand; every python column widened here closes exactly that class of gap for its own case. **NOTE added on merge into v5.9.0**: tpv36 was re-added after this entry was written (a separate, independent branch), bringing the final combined CI_CELLS to 20 of 27, not the 19 of 24 this entry measured against -- see v5.9.0's own Tasks-done row | branch `haruto/ci-matrix-optimization` |
| 2026-09-16 | v5.9.0 cut (minor, not a patch -- real new capability ships): item 19(a) closed for real this time. A fresh mira-volkov (the prior worktree was already reaped) re-derived the C_degen dynamics port from scratch, found the ACTUAL root cause of the reverted merge's regression (`np.einsum`'s BLAS-batched-matmul dispatch differs at the ULP level from its per-element form on a 3-term contraction -- a genuine NumPy association difference, backend-agnostic in principle, but only jax's nonlinear-dynamics amplification pushed it past a bound; the "JAX-graph-fusion" hypothesis passed to her was checked and did NOT hold, correctly reported as such), fixed it by restoring the exact pre-port einsum for non-wedge elements, and added the jax column her own probe had been missing (a real gap in her original verification, not just a hypothesis). Re-verified myself: `test.tpv8 x python-jax` back to 4.119873e-10 (exact historical match), fresh in her worktree before merging. Merged alongside haruto-nakamura's independent CI_CELLS widening (10->19 of 24, matrixing removed the shared-runner memory ceiling, 6 cells newly measured with `/usr/bin/time -v`) -- both branches based on stale/diverged points relative to each other (haruto's rebased cleanly onto the C_degen revert and dropped all tpv36 references; mira's re-added them), reconciled by hand in `testsys/matrix.py`/`CLAUDE.md`/`.github/workflows/test.yml` (auto-merge handled test.yml and pathway_forward.md cleanly; matrix.py's CI_CELLS comment and CLAUDE.md's cell-count line needed manual reconciliation to the true combined state: 20 of 27). Fresh full gate on the fully-merged tree: unit/regression/e2e ALL SUCCESS, 27/27 cells in 1599.5s, tpv36 numpy 7.894064e-09 / jax 5.867293e-09 against a bound of 1e-6 (~127x/170x headroom), fortran bit-exact against a freshly-frozen reference | v5.9.0 |
| 2026-09-16 | v5.8.7 cut, minutes after v5.8.6: v5.8.6's tag-triggered `publish.yml` reported `build-and-push` SUCCESS (image genuinely pushed) but `verify-published-image` FAIL -- a false failure, not a real one. Cause: a container built via `docker build` and run fresh on a LATER, SEPARATE job/runner has no working git network/credentials in that context (confirmed: `check_network_side`'s own, older, unrelated network checks in the SAME run correctly reported UNVERIFIED "tag on remote... offline?" and "gh not installed" for the identical reason). `check_tag_is_annotated`'s remote-peel fallback (added for v5.8.2) had not been given the same UNVERIFIED escape hatch -- it treated `git ls-remote` returning rc!=0 (a failed round-trip) the same as rc==0 with the wrong line count (a confirmed lightweight tag), both routing to a hard FAIL. Split them, matching `check_network_side`'s existing pattern for the identical class of problem. Verified locally both ways (a monkeypatched rc=128 now reports UNVERIFIED and does not raise; a real annotated tag still reports PASS via the fast local path). v5.8.6's GitHub Release notes corrected to say its image had, in fact, already published. Gate: unit+regression fresh green; e2e not re-run (test-harness-only change, zero src/testsys/case_input files touched) | v5.8.7 |
| 2026-09-16 | v5.8.6 cut (patch, not the v5.9.0 originally proposed -- tpv36/C_degen dynamics reverted for a real regression before this cut, see item 19(a); nothing feature-worthy ships here, only Docker/CI fixes). Root cause of v5.8.5's continued Docker failure: `ubuntu:22.04`'s `mpich` package depends on `libgfortran5` (runtime) not `gfortran` (compiler) -- `make` failed with `gfortran: command not found` inside the Dockerfile's own build step, so the image had NEVER actually built the solver. Fixed in both `Dockerfile` and `install-eqdyna.sh`'s ubuntu branch (identical latent gap, never exposed because every environment that ran it already had gfortran for unrelated reasons). Found and fixed a second issue one layer further in: `--no-install-recommends` gave the image a stricter dependency closure than `install-eqdyna.sh -m ubuntu` itself, silently dropping `mpif.h` -- flag removed. Also landed: `workflow_dispatch` on `publish.yml` (debug iterations no longer cost a release; push/`:latest` still gated on a real tag push), confirmed via a free dispatch run (`gh workflow run`) BEFORE cutting this tag; haruto-nakamura's CI matrix split (660s->303s, -54%, coverage unchanged at CI_CELLS, new `test_ci_workflow_coverage.py` guard) merged after confirming `paths-ignore`/`GH_TOKEN` survived and a pathway_forward.md numbering collision (two independently-created "item 35"s) resolved by renumbering one to 37. mira-volkov's C_degen dynamics + tpv36 merged, THEN REVERTED minutes later after a fresh full sweep found `test.tpv8 x python-jax` regressed 10x over its bound on a mesh with zero wedge elements -- isolated to her 3 changed kernel files by direct revert-and-rerun in both directions (see item 19(a) for the full account); a fresh mira-volkov re-dispatched with the isolation recipe and two additional findings (her verification probe never exercised jax; one candidate mechanism disproven). Gate: unit/regression/e2e all SUCCESS, 24/24 in 1462.5s, on the reverted (8-case) tree, fresh after the revert -- not reused from before it | v5.8.6 |
| 2026-09-16 | v5.8.5 cut, minutes after v5.8.4: v5.8.4's tag-triggered `publish.yml` failed AGAIN, same `--break-system-packages` flag, one step further down the pipeline (`publish.yml`'s own CI-only pytest/scipy install, both jobs). The v5.8.4 fix addressed the failing LOG LINE, not the PATTERN -- a repo-wide `grep -rn` for the flag at the time would have caught both remaining copies in one pass, same shape as the `.gitignore` inline-comment incident (a fix that looked complete because the symptom went away). Checked `install-eqdyna.sh:91`'s own use of the flag before touching anything near it: it is in the macOS/Homebrew branch (`brew install ... python`, which DOES enforce PEP 668), not the ubuntu branch (whose own pip install at line 71 has never had this flag) -- not a matching bug, left alone. Gate: unit+regression re-run green (fast tier only -- no src/testsys/case_input file changed since v5.8.4's fresh 24/24 e2e run minutes earlier, so that result still holds and re-running the ~20-minute sweep for a workflow-only change would have reproduced it byte-for-byte; recorded as a deliberate, reasoned skip, not a silently loosened gate). v5.8.3 and v5.8.4's GitHub Release notes both corrected in place to say their images never published | v5.8.5 |
| 2026-09-16 | v5.8.4 cut, minutes after v5.8.3: v5.8.3's tag-triggered `publish.yml` run (the FIRST time that workflow ever actually executed, gated on tags) failed at the Docker build step -- `pip3 install --break-system-packages ...` on a `FROM ubuntu:22.04` base, whose stock pip3 (22.0.2) predates that flag (added in pip 23.0.1 for PEP 668, which 22.04 doesn't enforce). Nothing had ever built this Dockerfile before; the gate v5.8.3 itself added caught its own Dockerfile's latent bug on its very first real build -- exactly the point of adding it. v5.8.3's tag was NOT moved to pick up the fix (already public: 158 unique clones/14d, 2 forks, a published GitHub Release) -- its Release notes were corrected in place to say the image never published; v5.8.4 is the first tag to actually publish `ghcr.io/eqdyna/eqdyna:v5.8.4`/`:latest`. Also fixed in the same push: `test.yml` ran the full ~11-min suite (591s real MPI compute) on every push including doc-only board/rules edits -- 3 of the 5 commits in the v5.8.3 chain were pathway_forward.md/PROJECT_RULES.md-only, burning 33 minutes for prose and delaying the release by the same amount. Added a narrow `paths-ignore` (pathway_forward.md, PROJECT_RULES.md, pastReleaseNotes.md, Docker.guide.md, docs/**) -- deliberately not a blanket `**.md`, since README.md is read by `test_readme_commands.py`/`test_release_complete.py` and stays in the trigger set. Gate: unit/regression/e2e green locally (fresh `install-eqdyna.sh` + `testsys/run.py all`) before this commit; branch-push `test.yml` green on the exact release SHA before tagging (rule 15); tag-triggered `test.yml` AND `publish.yml` (both jobs, including the fresh-pull re-verification) checked after tagging -- see this same Tasks-done row's own edit history / the session log for the actual result, not asserted here in advance of it | v5.8.4 |
| 2026-09-16 | CI matrixed: one job (apt+build+unit+regression+e2e-ci back to back, ~660s wall clock across 14 runs that day, e.g. run 35147230900) split into `build` + parallel `unit-regression` / `e2e-ci-fortran-a` / `e2e-ci-fortran-b` / `e2e-ci-python`, each downloading `build`'s `bin/eqdyna` artifact via `actions/upload-artifact`/`download-artifact` rather than rebuilding. Coverage UNCHANGED -- still exactly matrix.CI_CELLS, 10 of 24 cells; new regression guard `test_ci_workflow_coverage.py` parses test.yml's `run_e2e.py --ci` invocations and proves their union is exactly CI_CELLS with no gap and no overlap (caught a real bug in its own first draft: a `\` line-continuation in the YAML made it read both fortran jobs as covering all 8 cases, an 8-cell silent overlap, before the fix). `run_e2e.py --ci` now composes with `--backends`/`--cases` as a filter on CI_CELLS (previously ignored both), so the workflow never spells out its own copy of the cell list. `actions/checkout`/`actions/setup-python` bumped v2->v4 (both were flagged deprecated, forced onto Node 24). MEASURED on the pushed branch (`haruto/ci-matrix-optimization`), not this box: baseline ~660s (1 job) -> 450s (4 jobs, one `e2e-ci-fortran` running all 8 cases serially -- a GH-hosted runner's core count sits at/under run_e2e.py's `--jobs` reservation floor of cores-4, forcing budget=1) -> 303s (5 jobs, that fortran slice split into two ~4-case groups balanced by run 35150197402's own measured per-cell seconds: A=tpv29+tpv1053d+tpv104+tpv8, B=drv.a6+meng2023a+meng2023cb+tpv10). Run-to-run variance on the shared GH pool was real and visible: group A/B were 169.2/163.1s in the run the split was sized from, but 126.3/204.2s in the very next run that used it -- the split still helped (204.2s critical path vs the un-split 332.3s) but is not perfectly balanced; noted rather than re-tuned to a single sample. Net: 660s -> 303s, -54%. Widening CI_CELLS itself was explicitly NOT done here (separate, measured decision per matrix.py's own comment) and no bound/case/backend was touched. Investigated for the wider local-sweep-time question (not implemented, out of this task's surface): the core-budget allocator is already the fixed all-or-nothing Condition version with its own regression guard (`test_sweep_core_budget.py`) and was not leaving cores idle inappropriately on this box (default `--jobs`=cores-4 comfortably covers the current 24-cell sweep's ~48 peak core-cost); `globalvar.f90:109` initializes `writeCompTime=0` and nothing in `scripts/`/`case_input/`/`defaultParameters.py` ever sets it, so the per-stage `compTimeInSeconds` output gated at `eqdyna3d.f90:86` stays unreachable without editing source -- proposed as a follow-up (expose it as a settable case param or env var), not fixed here. Two runs used for verification: 35150197402 (first matrix, all 4 non-fortran-split jobs green) and 35151395127 (fortran split, all 5 jobs green) | branch `haruto/ci-matrix-optimization`, `.github/workflows/test.yml`, `testsys/e2e/run_e2e.py`, `testsys/regression/test_ci_workflow_coverage.py` |
| 2026-09-16 | v5.8.3 cut (resume-after-429 session): items 23 (Drucker-Prager kernel regression test) and 28 (TPV29 SCEC comparison script) verified fresh and closed, with a real correction found in the process (recorded Mw 7.45 -> 7.034, no reproducible provenance, 4.7x off in seismic moment); C_degen wedge-degeneration MESH ported to Python (dynamics stay refused -- an uncommitted, unverified dynamics draft was found in a stale worktree and deliberately NOT landed, preserved for follow-up); Docker publish workflow gated on the repo's own test tiers running inside the built image before push, plus a fresh-pull re-verification job (previously zero verification beyond the build succeeding); item 29 closed (all 4 git stashes confirmed unapplicable by construction, `stash drop` recommended not executed). Gate: unit SUCCESS, regression SUCCESS (one transient flake reproduced-then-cleared under heavy shared-box load, root-caused to a load/build-timing issue in `test_fault_geometry_guard.py`'s sequential build+9x-exec pattern, not a code regression -- confirmed via 2 clean re-runs at lower load and by reading `testsys/run.py`'s regression runner to rule out an intra-suite race), e2e 24/24 in 1332.1s (elevated by real external load on a shared box -- `train.py` at 99.7% CPU for 27h, unrelated; not recorded as a perf datapoint) | v5.8.3 |
| 2026-09-15 | v5.7.1: v5.7.0 was tagged on a local green that could not model a 7 GB runner -- the python backend needs 10.4 GB for tpv104 and the job was SIGTERM'd. Rule 15 now puts the tag AFTER CI green. Fourth CI red in one sequence, all the same shape (local green that did not transfer): what was committed, how the gate was invoked, what environment, what resources | v5.7.1 |
| 2026-09-15 | OPEN: python port memory. Same case (tpv104, dx=500, serial): fortran 0.95 GB, python-numpy 4.16 GB, python-jax 10.41 GB. JAX adds ~6 GB over NumPy; prime suspect is buffer donation, which a prior agent dismissed on TIME grounds (9 buffers vs 120 iterations) without measuring -- but 9 live buffers at ~900 MB is the gap. NumPy's own 4.16 GB is dominated by scat_val/scat_idx at 873.7 MB each (109M entries, 148 per element) because the port materialises the whole scatter for one bincount where Fortran scatters in place. Constraint from owner: do not trade throughput for footprint | v5.7.1 |
| 2026-09-15 | v5.7.0: Python setup 14.0->1.46 s (9.6x) byte-identical; JAX solver 2.1x (the jit was rebuilt inside run(), so every call paid a full ~14 s XLA compile -- GPU utilisation measured 1.6%); NumPy solver 1.90x bit-identical. jax-gpu end-to-end 31.3->9.44 s, i.e. 1.14x -> 3.8x vs Fortran serial | v5.7.0 |
| 2026-09-15 | e2e is backend-parameterized: the same gated cases run through Fortran and the Python/JAX standalone against the same references, delegating to the acceptance comparison rather than duplicating it. jax added to CI (after the numpy upgrade so it cannot be clobbered) | v5.7.0 |
| 2026-09-16 | v5.8.1 cut: per-stage timers shared ONE `startTimeStamp` across eight sites in six files, so `compTimeInSeconds(2)` was understated **511x** (0.0001 s vs 0.0511 s measured before/after on test.tpv8) -- not the "<=1% bias, harmless" item 30 recorded; each site now uses a local, output bit-identical. `case.setup` REFUSES `ntotft>1` with a named reason (it previously died in list-directed input, exit 2, which also made the deliberate stop at `meshgen.f90:790` unreachable). `test_stop_exit_status.py`'s real-binary probe had been silently SKIPPING on every tree (looked for `src/eqdyna`; install moves it to `bin/`) -- live again and now running in CI for the first time. `test_ci_dependencies.py` never scanned `src/python`. README's perf numbers corrected in three classes and a Performance section added where every figure names its command. Rule 16 had quoted a command CI does not run. Rule 17 + CLAUDE.md added (TPV revival workflow). Three claims closed BY MEASUREMENT: item 24(a) half-tractions do not reproduce (Tn 1.0018 over 4747 nodes), item 11 cannot diverge (structural + already aborts), item 32's additive flip model refuted (removing decomposition made flips go UP 415->442; drv.a6 has a fixed marginal population of ~400 roundoff-decided nodes). Gate: unit SUCCESS, regression SUCCESS, e2e 24/24 in 1163.8s, e2e-ci 10/10 in 139.7s; CI green on 106a8fc | v5.8.1 |
| 2026-09-16 | v5.8.0 cut: `src/fortran/` + `src/python/` split, Python solver restructured to mirror the Fortran 1:1 (14 modules, solver 1497->842 lines), friclaw 2 ported so the sweep reaches 24 cells with ZERO declared-unsupported. TPV 29 added to `swtwNucleation`'s smoothed list in Fortran AND the port -- test.tpv29 had been declaring `par.tpv = 36` to reach its own spec formula, hiding a port gap that nucleated 0 of 3321 nodes (7.10e7 -> 9.9e-15). Port stopped deviating from `driver.f90:30` (`force * inv_mass` vs DIVIDE): drv.a6 x python-numpy 461 -> 423 flips under its UNTOUCHED 450 budget. `accept` and `parity` tiers and the pydump instrumentation removed, ~5,700 lines net. A core-budget DEADLOCK in the sweep fixed (80 min, 14 of 24 cells unrun, no error) with a guard verified to reproduce it first. References are now one decomposition-independent `frt.canonical.txt` per case. Gate: unit 214, regression all, e2e 24/24; CI green on f7f95d6 | v5.8.0 |
| 2026-09-16 | v5.8.2 cut: test.tpv36/test.tpv37 run under MPI again -- `checkFaultMPIAlignment`'s DIVIDE-branch refusal (exit 51) was correct-but-unaudited; audited now (3416 fault nodes, tnrm/tstk/tdip ratio 1.000000, max\|diff\| 1.0e-08) and downgraded to a rank-0 NOTICE, pinned by new guard `test_dipping_fault_y_split.py`. `requireValidNeighbor` (code 52) added on both `mpi_sendrecv` sites. Interim `C_degen!=0` refusal added to the Python port; tpv36/tpv37 stay UNGATED (rule 17 step 7: never UNSUPPORTED-for-new). PRE-TAG AUDIT (victor-reyes + lars-eriksson) found and this release fixed: (1) CRITICAL -- `.gitignore`'s `scec_archive/` pattern carried a trailing inline comment, which gitignore does not support as a comment, making the whole line an unmatchable literal; `scec_archive/` was therefore NOT ignored and f2c9851 tracked 693 files / ~1.16 GB of fetched SCEC data into git despite every doc saying it stays untracked. Pattern fixed, files `git rm --cached`'d this release; the blobs remain in history pending a deliberate owner-approved rewrite (NOT done here). (2) the commit message's "3477 fault nodes" for the arn audit does not match the actual evidence (`3416`, recorded twice in code comments and in the regression guard's own printed output) -- 3477 was a transcription slip; 3416 is the reproducible number and is what ships in the code comments and this note. (3) stale case-param comments in test.tpv36/37 still described the guard as refusing rather than noticing; corrected. (4) the Python C_degen refusal message wrongly claimed tpv36/37 were "declared UNSUPPORTED in matrix.py" (they are not; correct answer is they are not gated at all); corrected. (5) `testsys/parity/evidence_tpv36_37_vs_published.py` hardcoded an ephemeral agent-session `/tmp` path (rule 4); now refuses without `EQDYNA_EVIDENCE_SCRATCH` set, consistent with the pre-existing spike-only-evidence pattern in `testsys/parity/`. (6) README's exit-code table regenerated after a hand-edit attempt broke `test_stop_exit_status.py`'s drift check -- fixed at the source (`errorCodes.f90` comment) and regenerated via `--update`, not hand-edited again. PROJECT_RULES.md's own index was one rule short (rule 17 existed in body, not in the index) -- added. DEFERRED, not fixed (needs owner judgment): purging the 1.16 GB from git history; the stale `v5.5.0 (cutting now)` / theme-vs-actual-content drift in pathway_forward.md's Release roadmap table (pre-existing, predates this release). Gate: unit 214 SUCCESS, regression SUCCESS (18 guards, after fixing the doc-drift regression this release introduced and re-running clean), e2e (`run.py all`) 24/24 in 1123.1s, e2e-ci 10/10 SUCCESS | v5.8.2 |
| 2026-09-15 | STANDING PROPERTY: the JAX GPU path is nondeterministic run-to-run -- XLA lowers the duplicate-index scatter-add to atomics. Measured 8.9e-08 on fric between two identical runs of UNMODIFIED code; jax-cpu is exactly 0. Bit-identity is unavailable on GPU and no gate may assume it; CI has no GPU so accept there runs jax-cpu and the 1e-8 tolerances hold | v5.7.0 |
| 2026-09-15 | OPEN: the accept tier has never exercised the NumPy backend -- test_standalone_acceptance.py:256 invokes the standalone with no --backend, so it runs the jax default. ~1,700 lines of port.py/port_rsf.py/port_tp.py are gated by nothing, and forcing --backend numpy gives 4/5 with drv.a6 at 461 flips against a 450 bound (pre-existing, confirmed by stashing). Needs a decision: is the bound jax-calibrated, or is there a real NumPy-path gap? | v5.7.0 |
| 2026-09-15 | v5.6.2: scipy used but never declared to CI -- third CI red in a row from the gate being green for a reason that did not hold in CI (v5.6.0 what was committed, v5.6.1 how the gate was invoked, v5.6.2 what environment it ran in). test_ci_dependencies.py closes the class; it found imageio unguarded on its first run | v5.6.2 |
| 2026-09-15 | Backend fallbacks removed from python/eqdyna/standalone: --backend jax no longer demotes to numpy, new --device auto/cpu/gpu,each run records the device it actually used. A numpy run had been labelling itself with an A100 it never touched | v5.6.2 |
| 2026-09-15 | Corrected a bad measurement: OMP_NUM_THREADS does not control XLA's CPU thread pool, so an earlier '1.17x on 16 threads, bandwidth-bound' claim measured nothing. With taskset pinning, JAX CPU scales 2.00x/3.96x/7.48x on 2/4/8 cores when compute-bound, plateauing ~2.5x when memory-bound | testsys/perf |
| 2026-09-15 | v5.6.1: bare `make` built nothing after the v5.6.0 clean-target fix removed the accidental `clean:eqdyna` dependency that install-eqdyna.sh relied on. `.DEFAULT_GOAL` pinned + regression guard. Rule 16 extended: gate with CI's own entry point, not a convenient subset -- v5.6.0 was committed complete, tree clean, commit re-verified in a worktree, and still went red because the gate never ran `install-eqdyna.sh` | v5.6.1 |
| 2026-09-14 | v5.6.0: fault-geometry validation (Python at case.setup + Fortran at read time), `scripts/convertFaultGeometry`, TPV29 50 m surface shipped | v5.6.0 |
| 2026-09-14 | v5.6.0: error-code system -- `src/errorCodes.f90`, 20 named codes 1-125, `abortRun` + `MPI_Abort`; 13 sites that exited 0 on refusal converted. Verified live: empty case under `mpirun -np 2` exits 21, no hang | v5.6.0 |
| 2026-09-14 | v5.6.0 pre-tag audit: 3 Criticals found and fixed -- full tier `KeyError` (unexercised since the fast tier never imports `full_specs.py`), validator blind to a global rescale, validator hiding local corruption up to 3000x tolerance | v5.6.0 |
| 2026-09-14 | Rule 2 sweep: a check that cannot run now FAILS instead of degrading. `dy` required not defaulted; grids too small for the y''' stencil refused; `getattr(par, ..., None)` removed. Removing the `dy` fallback exposed that the test fixture never supplied `dy`, so every offset assertion had been testing the fallback | PROJECT_RULES.md rule 2 |
| 2026-09-14 | TPV29 verified against the 2015 SCEC submission at matched 100 m: rupture time 0.006 s median, final slip 0.22% median over 24 on-fault stations | testsys/perf, scec_archive |
| 2026-09-14 | Speed at fixed 48 ranks is linear in cells/rank to 2.1% (100 m vs 50 m); the earlier refutation varied rank count, a different variable | testsys/perf/measured_runs.md |
| 2026-09-09 | GM/src-evol output every step; mpif90 on ubuntu | b1bc5e9 |
| 2026-09-09 | Project rule book seeded (14 rules); build artifacts + `scratch/` gitignored | 8c40871, 8414b5d |
| 2026-09-09 | Exec bits restored on `create.newcase`, `generateFaultInterface` (test suite was unrunnable from clean checkout — PATH-shadowed by EQquasi) | 27f8a76 |
| 2026-09-09 | v5.3.4 release audit run: build green, 4/5 reference cases pass; blocked on stale `test.tpv1053d` golden reference (uninitialized denormal captured pre-v5.3.3; HEAD == v5.3.3 byte-identical) | release not cut |
| 2026-09-09 | Root-caused tpv1053d frt.txt col 22 mismatch: old reference never wrote fric(23) (uninitialized denormal at all 2025 nodes) | 85d4d53 |
| 2026-09-09 | Fixed uninitialized `thetaPcTmp` in `NewtonRaphson` for friclaw==5 (src/faulting.f90); tpv1053d reference regenerated from the fixed binary, doubles as the rule-10 regression test | d9a50fa |
| 2026-09-09 | v5.3.4 cut: clean rebuild, full gate 5/5 SUCCESS (drv.a6, tpv8, tpv10, tpv104, tpv1053d) | v5.3.4 |
| 2026-09-09 | Built `testsys/` tiered test system (unit/regression/e2e, single entry point `testsys/run.py`); fixed check.test.py to gate on exit code and activate the dormant `.nc` comparison (items 3, 4); `testAll.py` now preserves `test/` as `test.prev/` instead of deleting it (item 5); `testCreateNewcase.py` migrated into `testsys/regression/` | 34691db, ae4162c, 17b9474, f847b3b |
| 2026-09-09 | v5.3.5 cut: fixed testsys/e2e/run_e2e.py (missing plotRuptureDynamics step), check.test.py's compare_nc_files (bit-exact `f1.identical(f2)` masquerading as a metadata check, false-failing on ordinary MPI floating-point non-determinism), scripts/lib.py (`sys.exit()` NameError in B2/B3), and install-eqdyna.sh (`chmod -R 755 scripts` rule-13 violation on every build, item 6); added install-eqdyna.sh to test_create_newcase.py's exec-bit guard; full gate 33/33 unit, 2/2 regression, 5/5 e2e cases (15/15 check.test.py comparisons) | v5.3.5 |
| 2026-09-13 | v5.3.6 cut: fixed PML region-14 cross-axis bug (item 8) and fric_tp_h=0 thermal-pressurization bug (item 12), both verified against independent oracles (static bug-pattern guard; clean-room v5.2.0 rebuild) and both reference-regenerated; landed the output-neutral refactor campaign (misc/ deletion, tabs->spaces, FRIC_SLOT_* map, func_lib.f90 extraction, MPI4arn/NodalQuant consolidation, Python post-processing dedup) verified bit-gated by clean rebuild + full e2e rerun; added vectorized python/ port (NumPy+JAX), parity claim flagged as spike-only and not yet in-tree-reproducible (new item 14); full gate green: unit SUCCESS, 3/3 regression, 5/5 e2e cases (15/15 check.test.py comparisons), ubuntu 22.04/gfortran 11.4.0/OpenMPI 4.1.1/netCDF 4.8.1 | v5.3.6 |
| 2026-09-13 | Release audit (v5.3.6..HEAD) flagged two gaps: standalone-port acceptance numbers asserted only in a commit message (no committed test), and the new `python/eqdyna/standalone` zero-Fortran port is new user-facing capability, not internal refactor — recommended v5.4.0 (minor) over the requested v5.3.7 (patch) per rule-15's escalation clause; both gaps closed before cutting (item 15, `accept` tier; makefile `pydump.o` rule deduplicated) rather than deferred | audit only, no tag |
| 2026-09-13 | v5.4.0 cut: PML boundary-inclusivity standardized + checkPMLAlignment guard (item 13), fric slot 6 retired, root cleanup (netCDF4 replaces xarray in check.test.py, orphaned testNameListWhole.py deleted), testsys parity/accept/perf tiers landed (item 14 + new item 15), python/eqdyna/standalone fully-standalone zero-Fortran port (tpv8/tpv104/tpv1053d) acceptance-tested at 8.23e-11/3.39e-07/3.36e-06 (`testsys/run.py accept`, fresh this run), lib.py lazy imageio import; full gate green: unit/regression/e2e SUCCESS (15/15 check.test.py), parity SUCCESS (NumPy+JAX+functional-neutrality), accept 3/3 SUCCESS; ubuntu 22.04/gfortran 11.4.0/OpenMPI 4.1.1/netCDF 4.8.1 | v5.4.0 |
| 2026-09-14 | v5.4.0 re-cut (same version, never pushed — local tag deleted and redone fresh, rule 15): standalone accept tier extended to 5/5 (item 18: tpv10 added at roundoff bound 3.02e-8; drv.a6 accepted under a documented two-part chaos-aware criterion after M10 Drucker-Prager viscoplasticity port + an RSF-nucleation bug fix), accept tier CPU-pinned (`JAX_PLATFORMS`) after a `jax[cuda12]` install rerouted it onto contended GPUs, new opt-in gpu tier (`run.py gpu`) and scaling harness (`run.py scaling`, not yet run — awaits an idle machine); release audit (item 21) found the drv.a6 criterion's derivation legitimate but two supporting numbers (the A/B flip counts behind its 450 bound, gpu-tier wall-clock timing) are still docstring/commit-message-only, not committed reproducible checks — logged as an open item rather than blocking. Fresh gate this run: `run.py all` unit/regression/e2e SUCCESS (15/15 check.test.py); `run.py accept` 5/5 SUCCESS (tpv8 2.20e-10, tpv104 6.76e-07, tpv1053d 1.10e-05, tpv10 3.02e-08, drv.a6 391/450 flips + median 0.0417s + phys_max 3.996e7, all within bound); `run.py gpu` SUCCESS (tpv8 7.33e-11 on this box's CUDA device, no timing printed); ubuntu 22.04/gfortran 11.4.0/OpenMPI 4.1.1/netCDF 4.8.1 | v5.4.0 |
